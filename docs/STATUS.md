# 📊 My Grind — Current Status

*Last updated: 2026-07-05 (NEWSLETTER SHIPPED + PLAN PICKER PRICE BUG KILLED + BLOG SEO PASS. 5 commits to `main` across 7/4-7/5, all deploys READY. PICKER: the 6/30 light remap made var(--white) near-black, so ALL FOUR plan prices were invisible (black-on-black) on the mandatory picker since card-required went live — very plausibly the 3-day signup drought; fixed with a hard-coded warm white + Coach's lock icon + "No charge for 30 days. Cancel anytime in one tap." line (`9f8d7ba`), live before Monday's Meta relaunch. NEWSLETTER: "Travel Ball Then and Now" published at /blog/travel-ball-then-and-now (Coach's copy verbatim, FAQ block + FAQPage schema, OG card, index/sitemap/short-slug rewrite, `25d1966`); email built into the Card Stack (docs/newsletter-2026-07-07-travel-ball.html), imported + SCHEDULED Tue 7/7 5:30am PT, subject "What 25 years did to youth baseball" (stat-curiosity style, no A/B, `627eba5`); affiliate links live in email + web (Pocket Radar, CamWood, Drills book) with sponsored tags + disclosure. BLOG SEO STANDARD (locked 7/5): one-line keyword dek on ALL posts (`8151a36`), answer-first boxes on high-intent posts (travel-ball + when-to-switch + after-12U + stats + what-is-a-journal, `303b197`; GPA guide already had one), prose keyword edits = propose-then-approve, E-E-A-T author schema permanent. GSC FINDING: several older posts were never indexed ("URL is not on Google") — explains NCAA pages owning all impressions; Coach requested indexing on 5 URLs 7/5. ADS: Coach paused Meta over the holiday, relaunch Monday 7/6 = the clean funnel test. DEFERRED: blog comments (Firestore-moderated approach agreed, later date). STILL OPEN: AllStars 0 of 15 as of 7/4 (Brandon nudge if still 0 Monday); weekend Daily Numbers rows; Wed open-rate read vs 6/30's 25.4%.) Prior 2026-07-02 (FUNNEL SEALED ON ALL PATHS + ONBOARDING WHITE & AUDITED + ALLSTARMYGRIND LIVE + SECURITY BACKLOG CLOSED + NCAA SEO PUSH. 9 commits to `main`, all deploys READY. FUNNEL: the "Just me" self path and fallback-kid parents bypassed the 7/1 card gate (it lived only on Screen 8) — `0573b86` gates the Screen 7 exit with the same mandatory picker and resumes the onboarding hand-off after checkout via mg_pending_onboarding; SAME COMMIT revived lockInCardReturnHandler, silently dead since 5/27 (bare showShareToast ReferenceError — mg_plan_picker_shown was NEVER set on checkout success, so paid parents could be re-prompted; yesterday's optimistic subStatus stamp never ran either). Card-required now covers ALL 3 signup paths. ONBOARDING: converted to always-white (`35fbed5`, palette swap + theme-color pinned #F7F4EF + gstatic preconnect + "PIN SETUPFOR" label fix) + light logo lockup (`b3bc4fa`, old art had a white "My" invisible on white) + FIRST DEDICATED AUDIT (`ce2c4bf`, 14 findings, 12 fixed): HIGH — re-running onboarding revived EXPIRED trials incl. expired 90/180 promo grants (guard now mirrors softball's, verified both directions); bare visits clobbered mg_signup_for on shared devices (now param-gated); PIN promised a login that doesn't exist (honest copy; real PIN gate = future build); position-picker desync on revisit; em/en dashes in player copy; softball bridge wrote the team-STATUS sentence as school; + self→fallback, signin-intent fallback route, 11th progress dot, dead data-mg-img applier, old-red rgba tints. TEAM OFFER: ALLSTARMYGRIND live (`7273f1b`) — Brandon Sonnier's AllStars, 3 months free via server-derived 90-day trial_end at checkout (NO Stripe coupon by design — coupons belonged to the retired payment-link flow; redemptions appear as Trialing customers, first invoice ~Oct 1), cap 15 in FOUNDER_CODES + cohort-store, PROMO_INFO now honors per-code `days`; link signup.html?team=ALLSTARS&promo=ALLSTARMYGRIND; branded offer-letter PDF + forwardable parent email delivered (Drive 03-Marketing). SECURITY: backlog closed (`b5b8f15`) — push-subscribe per-IP rate limit (read tier 60/hr), feedback-request requires verifyBearer (client sends bearer + friendly 401 prompt; landed BEFORE SMS_DRY_RUN ever flips), api/magic-link-request.js DELETED (216 lines, zero callers, was publicly callable + sending email); live-verified 404 + auth_required. SEO: NCAA push (`c344e95`) — answer-first box + 5-level requirements table (D1 2.3 w/ academic-redshirt band, D2 2.2, D3, NAIA, NJCAA) + 2 exact-match FAQs (schema now 8), homepage keyword anchors ("NCAA GPA requirements" from Playbook list + how-it-works step 2), sitemap lastmods, OG cover regen (closes the 6/29 item); Coach's manual step = GSC Request Indexing. THEME: light conversion COMPLETE — Season Goals card (Coach's catch) + 8 warm-maroon siblings (share/help modals, install + trial banners, coach/why cards, grade nag) remapped on light (`3de9428`); both-sport tab walk verified; softball toggle pale state investigated = INTENTIONAL locked-sport design, no change. Stripe connector verified live in-session (3 customers exactly; badimeo = code-signin identity, no Stripe presence, no revenue impact). STILL OPEN: verify first AllStars Trialing signups + cap count; confirm Uriel's $14.16 payout landed in Chase ••2761 (~7/3); GSC request-index click; Meta ad creative (Coach's footage, deferred); real player PIN gate (optional future); CSP report-only (deliberate).) Prior 2026-07-01 (CARD-REQUIRED SIGNUP SHIPPED EARLY + 30-DAY TRIAL + FULL APP AUDIT. 9 commits to `main`, all deploys READY. LATE ADD (post-eod-sync, `4028dfc`, from 3 real-user reports): (1) annual buyer still saw the trial-countdown banner — sync needs a signed-in Firebase token a fresh buyer doesn't have; showTrialBanner now also hides on subStatus active/trialing/past_due AND the ?co=success return stamps an optimistic subStatus='trialing' (Stripe still source of truth; affected existing device just needs one welcome-email sign-in); (2) theme toggle invisible in the installed app — fixed top:10px sat under the iOS status bar; now +env(safe-area-inset-top); (3) Screen 8 pill claimed "Invite sent" for EVERY non-fallback player even with no phone on file or a failed send (runtime logs showed ZERO invite-send calls behind an "Invite sent" card) — pill now only claims sent on a confirmed API 200 (player.inviteSentAt), else "Setup link ready" + Send button. OPEN: reproduce Coach's "sent the link, never arrived" with runtime logs watching — no /api/invite-send invocation reached the server today, so either a client-side share path was used or the send needs a retry to trace; also confirm SMS_DRY_RUN=false is actually set in Vercel env before trusting text delivery. MONEY FUNNEL: the 6/24 no-card revenue leak is CLOSED EARLY (`d96a99f` — the Meta "no card" ad was paused, removing the promise blocker; passed /phase5-preflight): the plan-picker "start my trial without a card" skip is deleted, the picker is mandatory, so every signup completes Stripe checkout = a visible card-on-file customer that auto-converts at trial_end. New coach-only `GET /api/admin/trial-count` (`fcbbb54`, Bearer ADMIN_RESCUE_TOKEN) counts the legacy invisible no-card cohort for Daily Numbers. TRIAL: 14 → 30 days in lockstep (`da6a30f`: STANDARD_TRIAL_DAYS, in-app TRIAL_DAYS cliff, both countdowns, all copy) and the audit batch (`e7255e1`) caught the last 8 stragglers including a REAL bug — onboarding.html's fallback was still 14, which could mis-expire a trial on days 15-30 — plus terms.html (legal), links hub, mg-config share captions x5, milestone share, founder JSON-LD, blog. PRICING: annual unified to $99.99 everywhere (was $99 / $99.99 / 99.00 inside one funnel; Stripe price is the truth). FULL AUDIT (3 parallel agents — security / stale-code / polish): security came back CLEAN, no critical or high findings (webhook signature, Firestore rules, admin/cron auth, COPPA managed-minors, magic links, no secrets in repo all verified); hardening backlog = push-subscribe IP rate limit, feedback-request auth BEFORE SMS_DRY_RUN flips, delete the dead api/magic-link-request.js. PWA: real /manifest.json (light splash matching the white default, maskable icon, install-sheet screenshots, "Log today" shortcut) replaced the stale dark data-URI manifest; sw.js v389 precaches the app shell + runtime-caches same-origin GETs so the journal now OPENS OFFLINE (/api/ never cached; cache prefix ybg-mygrind → mygrind). POLISH: pinch-zoom unblocked on index/signup/founder (WCAG 1.4.4), signin.html finally loads the brand fonts (was silently system sans), theme-color matches white on index+signup, cache headers added for /media/ (54MB had none), landing section headings deepened to --gold-dark #7A6224 (Coach flagged #A8873A as washed out at Bebas display sizes), Screen 8 player-card teasers now real ink on white (`e10eac3` — they had kept dark-theme tans + a 0.78 fade), landing lead screenshot RESHOT IN LIGHT THEME (`ae3419f` — 6-day streak / 490 reps / 4.2 check-in, caption "Light by default, dark when they want it", other 5 stay dark by choice; reusable harness at scripts/shoot-light-dashboard.html), Screen 8 honesty pass (`e0344ad` — Coach-feedback tease → "Academics & GPA" because coach notes are future Pro; button now "Resend setup link" + waiting-msg explainer; review prompt said "3-Day Streak" but fires at 7 → "One Week Strong", and its testimonial textarea was invisible on light — remap fix also repaired ~25 other washed inline-styled tans), welcome email fixes (no more "Payment landed" for $0 trial signups + a "open this email on your phone, add MyGrind to your home screen" tip, `4068144`). Repo CLAUDE.md stale facts rewritten; mockups/signup-gate.html deleted. LOCKED TODAY: card-required NOW not post-7/4; trial=30; Screen 8 teases only live free features; lead screenshot light / other 5 dark. STILL OPEN: build the card-required Meta ad creative BEFORE relaunching ads (old "no card" creative is a landing mismatch); read day-1 card-required signup data (Vercel visits + Firebase identities + Stripe customers); the security hardening trio; Coach decisions — signin light-vs-dark (last dark page in the funnel), "elite" inside recruiting features, /links in sitemap; Firebase-script defer in softball.html needs the ~line-14959 init-ordering check first; CSP stays Report-Only on purpose.) Prior 2026-06-30 (LIGHT/WHITE THEME SHIPPED LIVE + landing conversion overhaul. SHIPPED to prod: light/white is now the DEFAULT theme on mygrindapp.com (dark preserved as an in-app toggle only; landing + signup always white) via merge `1ad6133` of branch theme/light-premium + `80ad994`; gold deepened to #A8873A on light for AA. Journal now opens on a "Start Today" entry-type chooser (`4f45b5d`). Landing reworked for conversion: hero "Track Your Grind. / Log it. Own it." (slogan LOCKED) led by the compass mark icon (dropped "Now Live"); trial CTA used strategically (hero + pricing only); newsletter moved to bottom + Playbook under the Coach's note; signup plan step reframed "Nothing's due today. Here's your plan for after."; fixed unreadable-on-white copy (grey boxes, academics green, day-14 box); accurate plan claims (separate journals, removed not-yet-live "coach feedback by text", added free weekly newsletter); journal marketing screenshot reshot to the chooser; SW bumped v387->v388. COPY/THEME/UI ONLY — Stripe + money path untouched. LOCKED DECISIONS: (1) light/white default, dark = in-app toggle only; (2) brand slogan "Log it. Own it." STILL OPEN: measure signup lift (Vercel signup.html visits + Firebase identities; GA4 key-events stay 0 by design — no card at signup); finish light-mode coverage on deep app screens (Goals quick-pick panel etc.); 5 of 6 landing screenshots still dark by choice.) Prior 2026-06-29 (4 commits. SHIPPED: (1) closed the signup conversion leak end-to-end (`14b5cd6` + `a51a7b4`) — Screen 0 ("Who's this for?") no longer demands a card: the "I'll pay with my card" + 💳 chips became "Continue →" + "No card needed to start", the pill reads "No card required · Free for 14 days", under-18 billing line softened, stale "My athlete" button reference fixed to "I'm a parent". Unified trial/billing copy across signup + homepage: killed the day-14/day-15 contradiction (now "before your trial ends"), reframed the pricing section + Step 1 so a plan choice reads as an after-trial decision not a signup gate. COPY ONLY — the Stripe card-collection plan-picker + all signup logic untouched. LOCKED DECISION: signup entry stays card-free + welcome-first, one trial/billing story site-wide, never a specific cancel-day number. (2) Retitled the NCAA GPA blog post (`2bbf376`): was avg position 14 / page 2 for `ncaa gpa requirement(s)` at 0% CTR with "requirements" missing from the title — led title+meta+OG/Twitter/JSON-LD/H1 with the keyword + the answer (D1 2.3 / D2 2.2 core GPA), bumped modified date. STILL OPEN: deeper "no-card vs hold-a-card" model decision parked for /phase5-preflight (post-7/4); request-index the retitled GPA post in GSC; OG cover regen for that post; FTB 2023 reply (blocked on notice number, call 800-852-5711). Numbers: GSC climbing — Google "10 clicks in 28 days" milestone, NCAA post owns top impression queries; paid still 3. 6/29 Daily Numbers row logged. Prior 2026-06-27 (1 commit + heavy marketing/ops day): named-author E-E-A-T across all 10 blog posts (visible "Coach Mike Young, 35 years coaching" byline + Person schema linked by @id to #founder) plus `docs/geo-ai-baseline.md`; deployed to prod (commit `b826099`). GEO deeper pass: llms.txt + robots AI-bot rules verified live; AI-answer baseline recorded (MyGrind surfaces for none of the 4 target queries yet; "training journal app" is the unclaimed lane). Conversion tracking: `sign_up` + `begin_checkout` already GA4 key events; real gap = no `purchase` event on Stripe success (spawned background fix); Meta pixel 326334441984368 confirmed installed (PageView + CompleteRegistration). Meta ad day-3: Softball cheapest $0.19/LPV, Academics 76 LPV, Video 0 = kill. Built a daily auto-posting engine (`mygrind-daily-social-post`, 7am PT) + road-to-10k plan. Logo audit: Concept 1 (compass-as-G) retired to Desktop Official Brand Package/_DO NOT USE/; built+uploaded an all-gold mark (4af5ecc6) + white mark (7e52c839) to fix the blacked-out logo on dark gear. Today's "log it after the game" ad set built (baseball + softball stills + testimonial Reels, gold logo on gear) saved to Drive 03-Marketing-Social + Mac Photos. NEW LOCKED RULES: blog-byline E-E-A-T exception; MyGrind logo on hats/helmets/uniforms on all ads; gold/white logo on dark (never black); always make BOTH a baseball and a softball version; videos default ~12s. STILL OPEN: weighted/plyo-ball Track 1 (4 demo videos + PlyoCare gear card). Numbers: paid 3 (Uriel converted). Reminders: FTB 2023 due Mon 6/29; toggle Meta Video ad OFF; Reggie written release before paid use. Prior 2026-06-26: NCAA/NIL blog + newsletter + QOTD generator. SUPERSEDED DETAIL FOLLOWS: new blog post "New NCAA Eligibility Rules 2026: A Baseball & Softball Parent's Guide" (`blog/ncaa-eligibility-rule-change-2026.html`) targeting the fresh age-based-clock + NIL search cluster, with full SEO/GEO wiring (FAQPage schema, sitemap, llms.txt, blog index card + JSON-LD, OG cover, topic-cluster cross-links to the GPA guide + HS post); "NIL" written as one acronym; and a reusable "Today's Grind" QOTD card generator (`scripts/build_todays_grind_card.py`) in the editorial Playfair-serif style (Story/Feed/Square). NCAA/NIL newsletter finished + built into the Card Stack (`docs/newsletter-2026-06-30-ncaa-eligibility-nil.html`) and SCHEDULED in Mailchimp for Tue 6/30 5:30am PT (send time locked at 5:30am, the recurring "9am" was wrong). REGGIE SMITH on board: 7x MLB All-Star / 1981 WS champ / Aug 15 2026 Legends of Dodger Baseball inductee loves the app and gave verbal OK to use name+footage (facts web-verified); first Insta360 Flow 2 Pro shoot produced 2 branded clips (TikTok teaser + 35s IG AL-vs-NL strike-zone Reel) — written release still TODO before paid-ad use. Org cleanup: always-load `~/.claude/CLAUDE.md` non-negotiables, memory consolidated, single social home = Drive "03 — Marketing & Social", new rules (per-platform post sizes; brand fonts = two systems). Numbers: paid 2 to 3 (Uriel's $14.99/mo card-on-file trial converted). Prior 2026-06-25: Summer Proof campaign + NCAA/NIL newsletter draft, SW v387.)*

---

## 🌐 Where Things Live

| Surface | URL | Hosting | File |
|---|---|---|---|
| Pre-launch waitlist landing | https://mygrindapp.com | Vercel | `index.html` |
| Parent signup flow (8 screens) | https://mygrindapp.com/signup.html | Vercel | `signup.html` |
| Player onboarding | https://mygrindapp.com/onboarding.html | Vercel | `onboarding.html` |
| The journal app (live for beta) | https://mygrindapp.com/softball.html | Vercel | `softball.html` |
| Legacy app URL (still referenced inside `softball.html`) | https://youngsbaseball.github.io/mybaseballgrind/ | GitHub Pages | served via `mg-config.js` runtime override |
| SMS backend | `/api/send-invite` | Vercel serverless | `api/send-invite.js` |
| Static legal | mygrindapp.com/privacy.html, /terms.html | Vercel | `privacy.html`, `terms.html` |

- **Repo**: https://github.com/youngsbaseball/mybaseballgrind
- **Domain (live)**: mygrindapp.com (Vercel) — apex redirects to www
- **Cloud backend**: Firebase (`my-grind-b8486`) — Spark plan (free) — used for cloud sync of profile/entries/goals from `softball.html`
- **Mailchimp waitlist**: audience `73280b4c02e9bc56c7e633892`, tag `2216797` (subscribe form on `index.html` posts via JSONP to us8.list-manage.com)
- **Tech**: Hand-authored HTML files (CSS + vanilla JS inlined, no build step, no framework), Vercel serverless backend (Node ≥18, ESM), Redis-backed rate limiter

## 🛠️ Tech Decisions

- **No build step** — edits land directly. Pro: zero infra. Con: no linting, no automatic tests.
- **Single-file SPAs by design** — each HTML file is self-contained. State passes between files via `localStorage` and URL query params (`?name=Sofia&fallback=1`).
- **Service worker cache version** in `softball.html` — bump `ybg-mygrind-vN` every time you ship a meaningful update so users get fresh content.
- **`localStorage` is source of truth client-side** in `softball.html` (no auth/session backend yet for the journal app itself; signup flow's `api/send-invite.js` is the only serverless function so far).

## 🧱 Phase Status (Notion is authoritative — see "MyGrind Worldwide Launch HQ")

- **LAUNCH +25 (2026-06-12) — UX Bug Fixes + Landing Refresh + First Paid Ad Prepped** — COMPLETE. 4 commits to `main` (SW v349→v353), all deploys READY. **Fixed the worst real-user bug: bottom tabs going dead after rotating the phone** (`94dcc49`). Root cause found and verified in-browser — a phone in landscape is ≥768px wide, crossing the desktop-layout breakpoint and swapping the entire layout model (phone scroll-lock ↔ centered desktop column) on every rotation; iOS Safari leaves the hardware-accelerated fixed nav with a stale touch hit-region after that swap, so tabs render but taps land on nothing until a reload/sign-out (matches "I have to close and reopen the app"). Fix gates the desktop breakpoint behind `and (pointer: fine)`, so phones and tablets (pointer:coarse) stay in one single layout in both orientations and real desktops still get the column; added an `orientationchange` re-pin net. **Same commit doubled the inactive-tab contrast** (#5C544A→#9A8C76, 2.54:1→5.76:1) so the tabs are legible in sunlight. **Outdoor Mode** (`5ff96a1`): an optional Settings → App Preferences toggle (default OFF) that boosts contrast for the field without a light theme — `body.mg-outdoor` overrides the muted palette variables brighter and drops the diamond background pattern; persisted as `prefOutdoor`, cloud-synced, off-state restores every value (verified). **App chrome cleanup** (`d39c9de`): account bar slimmed to identity only, the duplicate Settings card removed + block relabeled "Help & Support", and 54 chrome emojis swapped to the mg-icon SVG sprite set. **Mobile nav seam fix + landing screenshot refresh** (`8ea126d`): closed an iOS sub-pixel seam at the top of the nav; rebuilt all 6 "See the app" screenshots from the live app with a seeded demo player and reordered them by what analytics shows visitors want (dashboard → game → NCAA academics → calendar → reflection → softball), retiring the stale 6-03 set. **GO-TO-MARKET:** the first $100 Meta (Facebook + Instagram) ad is fully built and staged for Coach to publish ~6/13 — 3 ads (Coach's real dugout reel / academics / softball), each in 9:16 + 4:5, copy blocks, and an Ads Manager checklist; UTMs `utm_campaign=first100` + `utm_content=video|academics|softball`; pixel `326334441984368` verified site-wide; day-5 read scheduled 6/17. **NUMBERS:** Firebase 7 total users; a new full-price $14.99/mo card-on-file trial (`cus_UgeTmWTgUJA5Af`, first charge ~6/25) is the strongest willingness-to-pay signal yet; 2 actively-charged subs. **Decisions:** none numbered; two standing rules saved to memory (Higgsfield creative rules; Meta over LinkedIn for paid). **Deferred:** the D1GRIND launch kit (carried, untouched — top queued build); Outdoor Mode real-world sun test (Coach); the ad publish itself (Coach, ~6/13).

- **LAUNCH +20 (2026-06-07) — Audit Closed + Push Live + Brand Unified** — COMPLETE. 3 commits to `main` (SW v324→v327). **Closed the 2026-06-05 audit's P2 tail** (`e5cc3c9`): (1) #258 pre-switch local backup — `mgClearLocalDataForIdentitySwitch(prevUid)` snapshots the outgoing account's journal to a KEEP-listed `mg_preswitch_backup:<uid>` before the purge (only if entries exist, pruned to 3) so unsynced entries survive a shared-device handoff; (2) #210-adjacent in-app upgrade 401 — `routeExistingCustomerToPortal` now treats 401/403 as a sign-in gap (stamps `mg_signin_intent`, routes to passwordless sign-in) instead of the dead-end error alert, so token-less payers can reach billing; (3) #210 **FOREVERYOUNG2026 server entitlement + real 10-cap** — new `api/redeem-lifetime.js` (public, rate-limited) atomically claims a slot via `redeemFounderSlot()` (caps FOUNDERMYGRIND 100 / FOREVERYOUNG2026 10, idempotent, rolls back over-cap) then writes a `sub:<email>` record via `grantLifetime()` (`status:active`/`plan:lifetime`/no expiry → `getSubscription` `isPaid:true` with no logic change); client redeems on the lifetime grant and rolls the optimistic local grant back to a 14-day trial on `cap_reached`; `syncSubscriptionFromServer` makes the server authoritative (DevTools/typed-code fakers reconcile to unpaid); (4) `?reset=1` now also clears `mg_plan_picker_shown`. **3-day-streak push opt-in prompt** (`049ca7b`): `mgMaybeShowPushPrompt()` fires once from `saveEntry()` when a save reaches a 3-day streak (gated on `Notification.permission==='default'` + not-subscribed + `mg_push_prompt_seen` unset) → a dismissible bottom-sheet whose \"Yes\" calls `enableDailyReminder()` (subscribe, `.ics` fallback) — the adoption lever since the Settings toggle alone won't convert. **Site-wide logo swap** (`3cc41f6`): home-screen/PWA icons (apple-touch/192/512/favicon), light-page `logo.png`/`logo-web.png` (black \"My\" lockup), marks, and `og-card.png` (dark lockup, 1200×630) all moved to the new black/white/gold set from `Brand & Logos/Official Brand Package`; caught + fixed a regression — `qotd-story.html` + `coach-reply.html` render `logo.png` on a dark bg, so repointed them to the dark-mode lockup (`mg-f153679d`) so the black \"My\" doesn't vanish (JSON-LD schema refs stay on `logo.png`); the in-app header `mg-f153679d` + `mark.png` were already the new lockup. **NON-CODE / INFRA: daily Web Push went LIVE** — flipped `PUSH_DRY_RUN=false` in Vercel + redeployed; verified `?test=1` → `sent:1` (real push to Coach's phone) and `?dry=1` → `totalEnabled:1`; the `0 1 * * *` (6pm PT) cron now sends for real (was log-only). **Decisions #66** (card-on-file picker stays off the onboarding-bound self + fallback-kid paths by design — they start the trial card-less and hit the paywall later), **#67** (push opt-in fires at a 3-day streak not first-entry — protects the one-shot, permanent-on-deny permission ask), **#68** (brand routing = dark-mode lockup on dark surfaces, light lockup on light). **Deferred:** backfill Coach's son + granddaughter FOREVERYOUNG2026 lifetime (waiting on their signup emails — not urgent, token-less users aren't downgraded); live-test the opt-in prompt in incognito (gated off on Coach's already-subscribed phone); CSP stays Report-Only on purpose.\n\n- **LAUNCH +19 PM (2026-06-06) — Premium Polish + Engagement Engine + Web Push** — COMPLETE. 12 commits to `main` (SW v321→v322). The app read "vibe-coded" and "took input but gave nothing back"; this session fixed both. **Premium polish:** warm-unified the brand palette to a warm near-black + gold canonical across 12+ files (239 grey swaps, 0 residual) and consolidated tactile button-press feedback into the shared `assets/interactions.css` (`56b4d16`, `66860e5`); trimmed fonts (dropped Inter) + added preconnect. **Bug + brand:** fixed the blank-page-after-Edit-Profile bug (`mgTab('profile',…)` → `mgTab('players',…)`) and rebranded the QOTD attribution from "The Grind" to **"MyGrind"** (one word, free branding on every quote impression) — `ccc2d65`; `build_qotd.py` fallback author + QOTD doc convention updated to match. **Engagement loop** (closing log → see growth → get rewarded → return): "Your Week" dashboard recap + a prominent streak hero + a dismissible install card moved out of buried Settings + on-brand milestone copy (`32a462b`); a dependency-free inline-SVG cumulative **AVG** trend chart (`8a76581`); a weekly **training-volume** bar chart, last 8 weeks (`3a92533`); **smart, context-aware journal prompts** above the free-text box — streak / recent-game / been-away aware with a daily-rotating fallback pool (`a08f207`); **achievement badges** beyond streaks (first entry/game/HR, 10 games, 10/25/50/100 entries) with a silent first-run backfill so existing users aren't spammed (`2542396`); an **AVG/OBP/SLG toggle** on the trend + a no-backend daily reminder (in-app nudge + downloadable recurring **.ics**) — `3d9b21f`. **Web Push (true closed-app daily reminder)** — Decision: push over SMS (cost, COPPA/minors, channel-burn). **Phase A** migrated the inline blob service worker to a real `/sw.js` file (push requires a file-based SW) + push/notificationclick handlers, with vercel.json serving `/sw.js` no-cache + `Service-Worker-Allowed: /` (`439ab11`); **Phase B** added `lib/push-store.js` (Firestore `pushSubscriptions`, one doc/device) + `api/push-subscribe.js` and folded the opt-in into the EXISTING Notifications card (`f241340`); **Phase C** added `api/cron/daily-reminder.js` (web-push send, prunes 404/410, `PUSH_DRY_RUN` guard, `?test=1`/`?dry=1`, daily/hourly modes) + the `web-push` dep + a daily Vercel cron `0 1 * * *` (`0a53eba`). **Push is ACTIVATED + verified** — VAPID env set in Vercel, `?test=1` returned `sent:1` (a real push landed on Coach's phone). **STILL gated by `PUSH_DRY_RUN=true`**, so the scheduled cron only LOGS. Push is opt-in only (browser requires an explicit "Allow" — can't be defaulted on; also correct for a minors audience). **Decision #34 updated** (QOTD attribution = "MyGrind" one word); **decision locked:** reminder channel = push, not SMS. **⚠️ SECURITY:** `CRON_SECRET` in Vercel had been set to a **Stripe live key (`sk_live_…`)** — caught via screenshot, rotated to a fresh random value, test re-passed; confirm `STRIPE_SECRET_KEY` separately holds the real Stripe key. **NEXT (the #1 morning task):** flip `PUSH_DRY_RUN=false` in Vercel + redeploy so the daily cron actually sends. **Deferred:** the go-live flip (left for a fresh, non-tired morning per CLAUDE.md); per-user reminder times (needs Vercel Pro + hourly cron); a well-timed in-app push opt-in prompt (adoption lever, since the settings toggle alone won't convert many).

- **LAUNCH +19 (2026-06-06) — Signup-Funnel Cluster Closed + P2 Batch + Newsletter/SEO + Email-Capture Fix** — COMPLETE. 8 commits to `main` (SW v317→v321). Closed the deferred high-risk signup-funnel cluster: self-path "Just me" + fallback-kid signups now create a Firebase account + send the sign-in code via a shared `finishSignup()`, and the signin `?auto=1` email-bomb is gated behind a first-party `mg_signin_intent` flag (`7a8a674`); the card-on-file picker is non-bypassable (flag set only on checkout success or an explicit skip, with an honest opt-out so re-showing can't hard-trap a no-card user) and an abandoned Stripe checkout returns to the signup funnel (`cancel_url`→`signup.html?co=cancel`, resumes at Screen 8) instead of dead-ending in the app (`2f107e6`; /phase5-preflight = GO). 4 P2 audit items: locked-player re-pricing freeze (count freezes once `playersLockedAt`), nav deep-link allowlist rebuilt for the 5-tab redesign, streak ledger `mg_activity_days` scoped per minor (`bee03cf`); admin rescue + founder-count endpoints rate-limited per IP before the constant-time token check (`677a312`). **Email-capture leak fixed** (`f96f45c`): the app's Mailchimp sync was a no-cors fetch Mailchimp could drop → switched to the reliable JSONP method the /newsletter form uses, and the homepage had NO newsletter form after the waitlist→live flip → restored one (both submit to the same audience). **Newsletter + SEO:** built the Jun 9 "Bigger Field, Bigger Decisions" issue on the Card Stack template (after-12U: travel ball vs patience + the HS-run-team decision; baseball + softball + girls-in-baseball); published its SEO blog post "After 12U: Travel Ball or Wait for High School?" (BlogPosting + BreadcrumbList + FAQPage schema; blog 6→7, added to index + sitemap) and a "Sign up for the newsletter" CTA + nav link on every blog post + index (→ /newsletter, feeds the audience) (`f7048ec`, `5747f54`); the newsletter header now uses the full MyGrind lockup. **Non-code:** set up Higgsfield with the current black/white/gold logo set (5 variants uploaded; old gold-only logo deprecated; `mygrind-visuals` skill + `higgsfield_mygrind_media` memory updated), produced the first MyGrind reel + branded still (dugout, glove + hands fixed via Flux Kontext, branded via ffmpeg, saved to `Marketing & Social/Higgsfield media/`); scrubbed YBG from the content skills (the `mygrind-content` edit held; the `ybg-content` delete reverted on the app's plugin re-sync → needs the Settings → Skills UI). **Decision #65 locked:** newsletter header = the full MyGrind lockup image, every issue (current black/white/gold set canonical; old gold deprecated). **Deferred:** P2 tail #194/#258 (shared-device sign-out purge + pre-switch backup), #266 (founder cap), #210 (lifetime server entitlement); in-app + live-site logos still the old gold; Higgsfield Brand Kit palette/tone lock; softball cut of the reel; QOTD elevation (carried from 6-04). **Live tests owed (Coach login):** self-path → a new Firebase Auth user appears, Stripe abandon → picker re-shows, two-account isolation on one device, the new homepage form → lands in Mailchimp. **Dependency (Coach):** check Mailchimp double opt-in / pending (likely why past signups "don't show up").

- **LAUNCH +16 (2026-06-03) — App Polish Marathon + Academics Redesign + Findability + Founder Ops** — COMPLETE. 8 commits to `main` (SW v309→v314), all verified live. **Interaction layer** (`40e223d`, new `assets/interactions.css`): real pressed/`:active` feedback + eased motion + `:focus-visible` rings + reduced-motion, linked into all 7 core pages — fixes the "taps fire but feel stale" complaint (mobile tap-highlight was globally off and almost nothing had an `:active` state); SW v310. **Landing premium pass** (`348717c`): elevated How-it-works step cards, richer gold CTA + pricing card depth, unified the dated 3-4px radii to the 10-16px language, scroll-reveal on below-fold sections; **+ founder-note copy fix** (index + foundermygrind): "the parents I see every weekend" → "the players and parents I see every day." **H/W growth log cloud sync** (`e8cd2bc`): `ybg_hw_log` was the last localStorage key not syncing — now pushes via `fbSaveMisc` with a union-merge (ascending by dateTs) on restore; SW v311. **Shared-device identity fix** (`4437f4e`): on a *different* account sign-in, clear the cached journal (canonical keys + every `mm:<minorId>:` namespace, active-minor dropped first) BEFORE cloud restore, so Account A no longer shows Account B's journal on a shared family device; gated on a stored prior uid so first sign-in / same-user / minor-switching are untouched; SW v312. **Signup-flow trim** (`14e277b`): cut the Screen 1 re-explainer (captured no input) + pulled the goals survey out of the funnel (now "Step X of 6"); a `STEP_ORDER = [2,3,5,6,7,8]` list drives the dots/labels/progress + `goNext` indices so no fragile 6.7k-line renumber; goals data was collected but never read, so effectively dropped. **Settings findable** (`c2a5302`): it was buried under Develop → Settings & Support; added a "Settings ›" row at the top of the Profile tab + a "⚙ Settings" link on the always-visible account bar; SW v313. **Academics card redesign** (`7ab43ba`): GPA-hero (52px) + a plain-language standing line replace the dense eligibility grid on the dashboard (the full NCAA grid stays in the Academics tab); **finals-window lock fix** — June (`_isFinalsWindow`) prompts "add your final grades before this locks" instead of falsely locking, Jul-Aug locks only once grades are entered, and the lock icon only shows when truly locked; **un-gated the NCAA core-course safety check to free** (`renderCoreAlerts` `MG_PRO` gate removed — per-class "may not count toward NCAA core" warnings + core-aligned summary + mark-as-elective are now free, the "coming Aug 2026" Pro tease tile hidden); SW v314. **Landing screenshots refreshed** (`f5af7db`): all 6 "See the app" images regenerated headlessly from the live app (puppeteer-core + system Chrome, 1080×1440) — the May-29 set showed the retired 6-tab nav + College-mode academics, the new set shows the current 5-tab nav + HS NCAA-core academics. **Decision #62 locked:** the NCAA core-course safety check is FREE (baseline trust, not an upsell); the deep multi-year NCAA core tracker (log prior years 9-12 → true NCAA core GPA + 16-course progress + per-school targets + junior-year lock-in alert) becomes the next Pro feature. **Non-code:** recovered the 2 drafted outreach messages (warm-client referral + coach "free for your team") + saved them to a Drive doc in Marketing & Social; built "The Daily Grind" daily founder-ops checklist (`~/Desktop/MyGrind Business/Daily Grind Checklist.html` — auto-resets daily, day-aware ROUTINE flags, one-tap tool links) + added a recurring "☀️ Daily Grind" calendar block (daily 8:00-8:20am PT). **Deferred:** multi-year tracker build (no aspirational UI until shipped), host the checklist at a private `/grind` URL (noindexed) for phone use, delete the old 8:00 "Start-of-Day Game Plan" calendar event (overlaps the new block), dead s1/s4 signup markup cleanup. **Smoke tests pending (Coach login):** signup funnel end-to-end → Stripe checkout, two-account identity isolation, H/W + check-in cross-device sync.

- **LAUNCH +16 PM (2026-06-03) — Signup Resume Fix + Reviews→Google + Brand Entity SEO** — COMPLETE. 4 commits to `main`, all verified live. **Signup resume fix** (`4fc81ac`): the AM signup trim's page-load init still ran `go(state.signupFor ? 1 : 0)`, stranding any returning user (saved `signupFor`) on the now-orphaned Screen 1 re-explainer ("Step 1 of 8"); fresh users were unaffected. Caught by walking the trim in a live preview. Fixed to `go(... ? 2 : 0)` (resume at s2, where both audience paths enter via `goNext(0,2)`); the `loadState()` 1→2 remap was dead code since init never read `currentScreen`. **Review prompt → Google Business** (`41bb4e6`): the 3-day-streak prompt's "Leave a Review" button was a `mailto:support@` (private feedback, no public rating) — swapped to the GBP review link (opens new tab); added an optional testimonial textarea that saves to the account-holder's Firestore doc (`users/{uid}.testimonial`, status pending; written to the adult's doc, never a managed-minor sub-doc, COPPA-safe) for curating onto the landing page; rotated the once-only key (`ybg_review_prompted`→`mg_review_prompted_google`) so existing users get one fresh ask; kept a private "Email Coach" path (no review gating, per Google/FTC). **Brand entity** (`5f5c278`, `e2ca99f`): expanded Organization `sameAs` 3→7 (added X @MyGrindapp, TikTok, YouTube, Crunchbase) to disambiguate from the poker app (mygrind.app) + AI-coach app (mygrind-ai.com); Crunchbase had auto-generated a correct, sports-categorized profile from the site's structured data (it's in "Sports Companies," "California Sports," "Private Santa Clarita" lists), now two-way linked. **Non-code:** deleted the old recurring 8:00am "Start-of-Day Game Plan" calendar event (AM-deferred); confirmed GBP is already a service-area business set to "United States" (Software company, no public address) so no change needed; drafted Crunchbase fill copy + descriptions in brand voice; Stripe (1 active $149.99 annual, $0 balance, no disputes) + Vercel (recent deploys all READY) health-checked clean. **Deferred:** Crunchbase verify/claim (needs Coach's @mygrindapp.com email + login) + adding the 5 socials via Actions→Edit, landing testimonial display block (needs a real quote), login-gated smoke tests (two-account identity, cross-device check-in/H/W).

- **LAUNCH +15 (2026-06-02) — Newsletter Funnel + 4-Platform Social Rebrand + Conversion Tightening + Blog Engine** — COMPLETE. 9 commits to `main` (7 this session + 2 pre-session morning: GA4 checkout funnel `aa07315`, nav redesign Option A 5-tab + Develop hub `16b30c9`). **Newsletter funnel:** new `/newsletter` signup page (email-only, no app account), GA4 `newsletter_signup` event, self-propagating subscribe links in the Card Stack footer (`c69d327`); single-annual copy fixed $99→$99.99. **Check-in cloud sync** (`75883c0`): `checkin.html` readiness+workload write to Firestore `users/{uid}` (managed-minor routing respected; doc-field storage so no rules change) — closes the 5-29 TODO; localStorage stays offline cache. **Above-the-fold hero CTA** (`6c1d29c`). **Phone optional at signup** Screen 2 (`7a9e30a`). **SEO** (`528ed78`): `/newsletter` in sitemap + homepage footer link + lastmod bump. **Blog +2** (`768a95e` baseball-training-journal pillar, `9f7437e` softball-training-journal) — both core keywords covered, schema + index + sitemap, blog 4→6. **Non-code:** all 4 socials rebranded to point at `/newsletter` (X = @MyGrindapp, 2022 age kept, pinned launch posts); Drive content bank (4 LinkedIn posts, 3 reels, 2 outreach msgs, 20 X posts + 10 reply-starters); Baseball Moms Uncensored comment; Metricool shelved (manual posting); 2026-06-07 X-batch reminder. **Decision #61 locked:** FOUNDERMYGRIND case-by-case, not public. **New memories:** foundermygrind_case_by_case, mygrind_x_handle, blog_content_roadmap. **Deferred:** signup screen cuts (Screen 1 re-explainer + Screen 4 goals survey → post-signup, renumbers flow), landing testimonial block (needs warm-client quote). **Smoke tests pending (Coach login):** check-in 2-device sync, signup with no phone.

- **LAUNCH +14 (2026-06-01) — Customer Portal Fix + Cloud Restore + Security Review + Twilio TFV Resubmit + GA4 Full-Funnel** — COMPLETE. 4 commits to `main` (SW v305→v308), all verified live. (1) **Stripe Customer Portal routing** (`462f949`): `goToPurchase`/`goToCoachPurchase`/`goToTeamSponsorPurchase` in-app branch now routes an existing customer to the token-gated Customer Portal instead of a fresh Payment Link (closes the parallel-subscription risk from the card-on-file ship); only a genuine 404 `no_customer` falls through to a Payment Link, and any other portal error fails closed. (2) **Cloud restore full history** (`45e160a`): `syncFromCloud` paginates the entries collection by document id (400/batch) instead of `orderBy('savedAt').limit(100)`, so a fresh-device restore pulls every entry, not just the last ~3 months. (3) **Cleanup** (`dc1cf57`): removed dead `syncToCloud()`, fixed the stale `get-subscription.js` "not yet auth-scoped" comment, simplified the `subscription-store.js` guard. (4) **GA4 full-funnel tracking** (`c78e344`): gtag.js (`G-5HLS0F06CQ`, same property as the app) added to index/signup/onboarding — only softball.html had GA4 before, so ad traffic on the landing/signup pages was invisible; fires a `sign_up` event at the confirmed new-trial moment (tagged plan + sport). **Security review** (manual, in-session): clean, no critical/high across billing/auth/minors; secret scan clean; Firestore rules uid-locked; webhook signature-verified; open follow-ups = firebase CLI rules deploy (drift risk) + billing≠login portal edge. **Twilio TFV RESUBMITTED** (ticket #27140216, TFV HH0d15f0e6c6fdecd2edab4b730ed83204): Huvi confirmed BRN/EIN valid and asked to resubmit; submitted via the console with a public Drive folder (CP 575 + stamped Articles + Cert of Status + SMS consent screenshot) in Additional Information; replied to Huvi; awaiting manual review; SMS stays `SMS_DRY_RUN=true`; 7-day check scheduled (`twilio-tfv-status-check-jun8`, 6-8). **GitHub 2FA** enabled on the `youngsbaseball` account (TOTP + recovery codes saved; mandatory by 7-16 or push access is limited → would break the Vercel deploy pipeline). **GA4 added to /morning-sync** (dashboard glance + connection map). **Expense logged:** The Signal $100 DBA newspaper publication (Legal/Filing; YTD $939.58→$1,039.58, deduction total →$12,741.16, est. tax savings ~$2,803; FBN publishes 6/3 through 6/24, separate from the $41.75 county filing). **Social:** LinkedIn + Facebook player-dev-tip posts + a Higgsfield dugout hero, saved to Drive. **Deferred:** begin_checkout + purchase GA4 events (checkout has 3 entry points; purchase confirms via the webhook), GA4 Data API auto-pull (needs a service-account grant), firebase CLI rules deploy, billing≠login portal edge.

- **PLAYER-EDUCATION LAYER (2026-05-30) — NEW, LIVE.** First in-app development reference, shipped in `softball.html` (commit `8f9bf95`, SW v296). **"The 5 Tools"** panel under More: hit/power/run/arm/field + a separate Pitching card + a "which tools matter for your position" guide, joy/dream-first copy ("the numbers are a map for the dream, never a fence"), benchmark tables (HS Varsity/JuCo/College) that show only the active sport via the toggle. **Hit & Power are production-first** (velocity tables removed); Run/Arm/Pitching keep numeric tables. **60-yard dash keeps a dated history** with PR + faster/slower trend, sport-aware (softball = home-to-first), separate logs + validation per metric, and now fills the previously-empty Stats 60 field. **Arm Health & Growth Plates** education card added to Resources (Pitch Smart for baseball, sports-med windmill-workload note for softball). **Resources split by sport** (added PGF + The Alliance Fastpitch; Girls-in-Baseball baseball-only); Get Help moved to bottom; hero clipping fixed. App content, **intentionally not indexed** (robots-disallowed). **Decision #60 locked:** 5 Tools = production-first + joy-first. **SEO note:** the 5-29 GSC homepage-indexing lever is RESOLVED (homepage indexed); branded search is healthy on the **www** property (5 clicks / 40 impr / 12.5% CTR / pos 3.9); the 4 blog URLs were request-indexed 5-30 (use the www/Domain property in GSC, not non-www).

- **SECURITY AUDIT + HARDENING (2026-05-29 PM) — COMPLETE.** Front-to-back audit (3 parallel auditors + manual verification on Opus 4.8). Foundations verified solid: Firestore rules lock every user incl. minors to their own data, no leaked client secrets, no committed creds, Stripe webhook signature verified, code/magic-link auth sound. 5 commits to `main` (`1995d10` `6dca7f4` `8ab3551` `77944ce` `57f8f94`), all deployed + verified live. **Closed:** (1) Stripe webhook returned 200 on handler error (silently dropped onboarding) → now returns 500 so Stripe retries; handler idempotency confirmed. (2) Admin rescue token traveled in the URL query string → now header-only (`Authorization: Bearer`). (3) No security headers → added X-Content-Type-Options/X-Frame-Options/Referrer-Policy/HSTS/Permissions-Policy + a Report-Only CSP. (4) **CRITICAL — three by-email endpoints (`get-subscription`, `stripe-portal-session`, `feedback-list?parent=`) returned a customer's billing/subscription state and a minor's coaching history to anyone who knew the email/phone, no auth.** Closed via new `lib/access.js` owner-scoping: a Firebase ID token's email must match the requested email. Staged behind `ACCESS_TOKEN_ENFORCE` (defaults ON; kill switch = `false`); clients send the token (softball.html waits for session restore; signin.html on sign-in restore). Phase 2 enforced live and verified: anonymous → 401, signed-in happy path confirmed working by Coach. **Perf:** ~633 KB of base64 logo/images externalized to content-hashed `/assets/img/` with immutable cache (softball.html 1.5MB→1.17MB, shared logo cached once across 5 pages). **Residual (documented):** `feedback-list?player=<phone>` stays open-but-PII-stripped until SMS verification gives a phone↔identity link; CSP kept Report-Only by recommendation (unsafe-inline needed → low enforce value, high live-flow risk; nonce-based CSP is a future refactor); signup.html left auth-free by design (routes auth through signin.html). ⚠️ Rotate `ADMIN_RESCUE_TOKEN` — old value was committed in docs/ROADMAP git history. New memory: `security_audit_2026-05-29`.

- **LAUNCH +11 (2026-05-29) — Landing Conversion + SEO Overhaul + Blog Launch** — COMPLETE. Builds on the 2026-05-28 Path B landing rebuild (7 commits: `a849bb4` split the public landing from the founder URL; `6015214`/`ed41c2b`/`0a2897f`/`b904c7b`/`98e9572` added SEO chunks — semantic H2 + sitemap/noindex, SEE THE APP grid, HOW IT WORKS, PRICING, visible FAQ accordion). Today's 11 commits: **Real screenshots** — replaced 5 identical placeholders, then swapped to 6 richer in-app captures (game, arm care, gym, reflection, off day, academics) normalized to a uniform 3:4 `object-fit:cover` grid; banners exposing a test email + pricing pills cropped out (`34030fd` + `a847a3c`). **Hero typewriter removed** (`7f7813d`): char-by-char typing + the 3.2s logo zoom that landed ~11s after load replaced with a fast staggered fade-in (settles ~1.2s); headline renders immediately (LCP), reduced-motion path hardened. **HOW IT WORKS reframed** "more than a tracker" (`d919e90`) and **moved above SEE THE APP** (`515c338`) for explain-then-show. **Bottom form-section removed** (`5103bd2`): the redundant third trial CTA + the Mailchimp "Get Updates" email capture + its ~125-line JS handler deleted; the founder note now carries the single closing CTA. **FAQ overhaul + single pricing CTA** (`0ac384b`): FAQ rewritten to 8 real parent questions (Grindr dating-app disambiguation kept first for SEO, data security, who-sees-the-journal vs the weekly digest, no-phone/email access, no-selling/ads, cost, no-App-Store) with the visible accordion and the JSON-LD FAQPage kept in sync; pricing consolidated from three identical per-card buttons to one `Start My Free Trial` CTA (consumer-subscription pattern, and all three old buttons already linked to the same `/signup.html`). **CTA copy** tightened to `Start My Free Trial` (`c0f292b`). **Age + founder name removed** from the visible What-is-MyGrind answer and the `audienceType` field (`921007b`) so copy does not cap age or use the founder's personal name; the name remains only in the hidden Person SEO node (Coach's call — it powers the Knowledge Graph and is invisible to visitors). **SEO hygiene** (`472e058`): sitemap lastmods refreshed (/ = 5-29, privacy/terms = 5-27), and `legacy-app.html` + `signin.html` + `coach-reply.html` noindexed/disallowed. **/blog launched** (`a9cc533`): a hub plus 3 SEO articles (`baseball-softball-stats-to-track`, `ncaa-eligibility-gpa-guide`, `how-to-keep-a-training-journal`) with shared `assets/blog.css`, BlogPosting + BreadcrumbList schema, sitemap entries, and a homepage footer link; articles cross-link each other and CTA to the trial. On-page technical SEO confirmed strong (full OG/Twitter cards, canonical, single H1, 5-node JSON-LD @graph, founder URL noindexed). All commits pushed; production verified live (FAQ live, CTA copy, no visible name/age). **Known follow-ups:** dead form CSS still inert in the stylesheet (cleanup deferred); no above-the-fold hero CTA yet (Coach deferred); app-wide name sweep of softball.html/signup.html still optional; Google Search Console reindex + sitemap resubmit pending (manual).

- **LAUNCH +9 (2026-05-27) — Code Auth + COPPA Cleanup + Card-on-File + Under-13 Parent Path + Pre-Ad Polish** — COMPLETE. 24 commits to `main` — largest single-day ship since LAUNCH. **Code-based 6-digit sign-in replaces magic-link** (`2ef4c8c` + `31faa26`): new `/api/auth/request-code` + `/api/auth/verify-code` endpoints, Redis-hashed codes with 15-min TTL, per-email + per-IP rate limits, signin.html UI rewritten to two-step email→code flow. Magic-link verify endpoint kept alive so admin rescue endpoint still works as backstop. **COPPA exposure closed:** 13+ posture aligned across public marketing (schema "9-22" → "13-22" in `69938c6`), signup (under-13 tile removed + age min 5→13 + dead `isKid` branch removed in `4d03112`), and journal. Privacy policy §5 vendor list expanded (`86014c0`), §6.5 COPPA section with 5 subsections added (`335acdb`). Under-13 Parent-Managed Account flow live (`d1b79fb` + `5b4b6a2` + `ea6c570` + `740e858`): Stripe charge on Family plan serves as FTC-accepted verifiable parental consent (industry pattern from Khan Academy Kids / ClassDojo / Apple Family Sharing). New endpoints `/api/managed-minors-{create,list,delete}`, `MgMinorScope` localStorage shim auto-namespaces reads/writes per child, profile switcher bar in softball.html when parent has child profiles, `docs/COPPA-RUNBOOK.md` documents parental access + FTC inquiry handling. **Card-on-file at signup** (`369210a`) replaces Day-11 in-app banner: Stripe Checkout `setup_intent` mode, auto-charge at trial end. Banner + all Day-11 capture logic removed from softball.html. Privacy + terms + landing copy swept for card-on-file language (`bb8f632`). **Firestore rules now version-controlled** at `firestore.rules` (`c9c4e9a` + `740e858`) — mirror of live Console rules with new `managedMinors` rules. Coach manually published to Firebase Console after each change (CLI not yet wired). **Plan picker readability fix** (`46bbded`): `var(--gold)` etc rendered transparent because signup.html had never defined the CSS variables at :root. Defined MyGrind palette at :root + hardcoded plan-picker hex values. BEST VALUE + RECOMMENDED pills and COPPA banner now visible. **QOTD audit fixed two coach-spotted factual errors:** "no batter on first" → "the runner on first" (`50cc587`), "rise ball...behind the mound" → "in the bullpen" + "seventh inning" → "late innings" (`0fb2e59`). **First-signup routing** (`2873083`): new users land on Profile (panel-players), tap "Continue to Academics" sets `mg_visited_academics` flag, every subsequent load defaults to Journal. Old `isProfileComplete`-only gate preserved as alternate graduation path. **More-section emojis swapped for Lucide icons** (`92636d7`): graduation-cap (Resources), bell (Daily Reminders), credit-card (Subscription), cloud (Save to Cloud — new `mg-icon-cloud` symbol added), mail (Send Feedback). requestNotifications success state rewrites button via innerHTML with SVGs instead of textContent emoji. **Coach Feedback chips plain-English rewrite** (`bb36e63`): 8 labels updated across coach-reply.html + softball.html, all `data-key` / `data-cf-focus` / `data-cf-sit` values preserved so SMS payloads and downstream displays keep working. **Pre-ad em-dash sweep on landing + signup** (`efa74d5`): 8 user-visible em-dashes converted (commas/periods/colons). **Default journal entry** Reflection → Practice (`b971dab`). `applyAgeAwareJournalPrompts()` and 3 call sites fully removed once static HTML defaults verified to match. **Coach Feedback audit shipped:** `docs/COPPA-AUDIT.md` committed (`48f729b`). SW cache v288 → v294 across 6 bumps. **CRITICAL FIND mid-day:** `STRIPE_SECRET_KEY` env var in Vercel contained a publishable key value since project setup; the only endpoint reading it (Day-11 banner) never fired in production so nobody noticed. Coach pasted correct `sk_live_…` value, redeployed, smoke-tested working with `cs_live_…` returned. Stripe keys also scoped to Preview env target. **6 LOCKED Decisions** (#52 code sign-in, #53 13+ posture, #54 card-on-file at signup, #55 under-13 parent-managed path on Family plan only, #56 always do industry standard, #57 first-signup Profile → Academics → Journal routing). **6 new memory files**: `feedback_always_industry_standard.md`, `coppa_under_13_parent_path.md`, `stripe_secret_key_misconfig.md`, `admin_rescue_endpoint.md`, `feedback_links_and_copy_blocks.md`, `advertising_launch_2026-05-28.md`. Production smoke tests all green: `/api/create-checkout-session` returns `cs_live_…`, `/api/auth/request-code` returns ok, `/api/admin/signin-link` returns ok, privacy §6.5 renders, schema "ages 13-22". **App ready for paid + organic advertising launch 2026-05-28 morning.**

- **LAUNCH +8 (2026-05-26) — FBN Approved Same-Day + Brandon Bug Fixed + SEO Disambiguation Live + Founder Cohort Tracking + Post-Payment Delivery Hardened** — COMPLETE. Six commits to `main` (3 earlier-morning Stripe webhook hardening: `7d98ca6` post-payment auto-Auth + welcome email creation, `0dddea6` per-email idempotency, `6d20636` Resend-rejection detection + stale-flag recovery; 3 in-session: `c7fcafe` founder cohort tracking infra, `3d02b55` Brandon sign-in fix + SEO disambiguation, `d9d5e11` sitemap lastmod bump). **FBN approval same-day** by LA County Registrar: Filing #2026114769, $41.75 paid via VitalChek, electronic certification, $41.75 logged to Notion expense tracker (YTD $939.58, projected tax savings ~$2,781). **Bank DBAs added** to Chase Business Complete Checking: MYGRINDAPP + My Grind app now legally tied to the LLC account. **Brandon's sign-in bounce bug** root-caused: softball.html only trusted localStorage, no Firebase Auth check before bouncing to /signup.html. Fix: signin.html appends `?signed_in=1` on all 4 redirects to journal; softball.html checks the marker OR `firebase.auth().currentUser` before bouncing — only truly anonymous visitors get sent to signup. SW cache v287 → v288. **SEO disambiguation** for Grindr/MyGrind fuzzy match: new "Is MyGrindapp a dating or social app?" FAQ question prepended to FAQPage schema + WebSite schema alternateName variants (MyGrind, MyGrind App, MyGrindApp, mygrindapp.com). **Google Search Console**: homepage IS indexed under the Domain property `mygrindapp.com` (previous "0 indexed pages" panic was looking at the non-www redirect URL; real canonical at https://www.mygrindapp.com/ shows "URL is on Google"). Sitemap re-submitted, reindex requested with new schema, branded query update expected in 24-72hr. **Founder cohort tracking** live in Redis: `lib/founder-cohort-store.js` + `start-trial.js` writes promoCode to Set + new `/api/admin/founder-count` admin endpoint returns counts/caps/remaining for FOUNDERMYGRIND (100) and FOREVERYOUNG2026 (10). Track-only by design; hard cap enforcement deferred. **Twilio TFV reply** drafted in Gmail thread (Huvi at support+id0030XR-6YX7X@twilio.zendesk.com) — awaiting Coach to attach CP 575 + stamped Articles of Organization PDFs and send. **The Signal SCV** picked for FBN newspaper publication at $100 (payment pending their receipt confirmation + spelling check). **CapCut platform scripts** written for TikTok / IG Reels / IG Stories / LinkedIn using the 5 real app screenshots Coach shared; 4 Higgsfield hero images generating (~4-12 credits total spend on Plus plan balance of 23.76). **Memory updates:** `llc_entity_details` (FBN approval + bank DBAs), new `vercel_project_info` (project + team IDs locked so future Vercel links don't 404). ROADMAP entry logged for iOS PWA Home Screen sign-in handoff bug (Brandon's earlier separate report — Apple-side limitation, defer to ~30+ founder milestone).

- **LAUNCH** — LIVE as of 2026-05-18 10:49am PT. `index.html` flipped from pre-launch waitlist to "Now Live" with primary CTA `/signup.html?promo=FOUNDERMYGRIND`. First customer trial loop discovered and fixed within hours. End-to-end sign-in flow verified in production by Coach.
- **Cross-Device Sign-In V1** — COMPLETE 2026-05-18 PM. New `signin.html` passwordless email-link flow live and verified end-to-end. Coach completed full round-trip on production (request → Gmail → tap link → land in journal). Firebase Console configured: Email/Password + Email link (passwordless sign-in) enabled, Authorized domains include `mygrindapp.com` + `www.mygrindapp.com`. Entry points wired across signup Screen 0, softball welcome screen, landing footer, trial-abuse overlay, Settings Sign Out card. Cloud backup banner + Settings card auto-prompts existing local-only users to push journal entries to Firestore on first sign-in. Branded Resend email + one-click Stripe portal both built but rolled back same night to fit Vercel Hobby 12-function cap; recovery via endpoint consolidation queued.
- **Phase 2 — Parent Signup Flow** — front-end complete (8 screens in `signup.html`). No backend wiring yet.
- **Phase 3a (Skeleton + Auth)** — COMPLETE
- **Phase 3b (Twilio SDK + Live Send)** — code complete, awaiting Twilio review on Round 3 TFV resubmission filed 2026-05-19 PM with stamped LLC info (Entity B20260231794, EIN 42-2579197, Business Type Private Profit, DBA MyGrindapp). All 4 prior rejection errors addressed: 30484 (legal entity match — LLC stamped 5-19), 30489 (website not established — live + indexed + privacy/terms with SMS sections), 30506 + 30513 (opt-in workflow — consent modal strengthened with HELP, frequency disclosure, inline privacy link in commit `d0e1df1`). Expected review: 3-5 business days. SMS stays in `SMS_DRY_RUN=true` until approval. The new Send Setup Link modal on signup.html Screen 8 (commit `aeb4038`) makes TFV approval an enhancement rather than a launch blocker — parents have 4 working ways to deliver the onboarding link without Twilio.
- **LAUNCH +7 (2026-05-25) — Conversion Fix + First Real Customer + LLC Bank + Newsletter Voice Locked** — COMPLETE. (1) **Critical conversion bug fixed** (commit `12b0f64`). Diagnosed mid-morning: signup.html buildScreen8 wrote localStorage state but never created a Firebase Auth user and never triggered a magic-link email. Every user who completed signup since 5-18 launch silently bounced unless manually rescued via admin endpoint. Aaron (5-19) was the only Firebase Auth user from organic signup, and only because Coach rescued him manually. Fix: Screen 8 entry now auto-redirects self-path users to `/signin.html?email=X&auto=1` which fires `sendSignInLink()` on DOMContentLoaded; parent-path users get an injected "ONE LAST STEP" banner with the same CTA so the kid-invite tile is preserved. Reuses signin.html's existing branded-Resend-first / Firebase-fallback chain. SW cache v286 → v287. Stranger-phone walkthrough verified working mid-morning. (2) **First real revenue:** $149.99 Family Annual subscription from inner-circle demo cohort. Customer paid full price instead of using FOUNDERMYGRIND (6 months free) — intent signal. Net $144.68. (3) **LLC banking compliance done.** Stripe payout destination swapped from personal Chase ••9395 / routing 028000121 to Chase Business ••2761 / routing 322271627. Personal account removed. All future revenue routes to LLC = clean 1099-K reconciliation for Sam + LLC liability shield preserved. Jun 1 payout of $144.68 confirmed routing to Chase Business. (4) **Twilio TFV human review engaged.** Huvi from support-onboarding (ticket #27140216) replied requesting CP 575 + stamped Articles + Cert of Status. Coach sent all 3 attached. Now awaiting actual human review. (5) **Newsletter voice approach locked** (Decisions #49 + #50). Hard-edged self-accountability, no escape hatches, headers don't apologize, span all age levels through college as consequence layer, app integration must be pain-point-specific not generic. Saved as 2 new feedback memories. (6) **Mailchimp dead-audience decision locked** (Decision #51): "My Grind Weekly" (829103) keeps existing campaign history, gets renamed `[ARCHIVED]` rather than deleted; CSV diff confirmed 0 unique members would be lost; landing-page delete-block + Welcome automation dependencies made full delete too costly for $0 benefit on free plan. (7) **Part 2 newsletter rewritten 4x** to hit the new voice bar — final version baseball-context-only, all age levels woven through, app-integration scenarios specific, ready to paste into Mailchimp draft. Part 3 also prepped with same treatment. (8) 1 doc-sync commit (`408ae00`) cleaned up 5-24 STATUS/ROADMAP residual. 2 commits total on `main` today.\n\n- **LAUNCH +5 (2026-05-23) — Resources Panel Refactor + Girls in Baseball + Stripe Tax Upload + LA Heat Partnership** — COMPLETE. (1) Stripe 1099-K TIN switch unblocked via document-upload bypass at Dashboard → Settings → Tax Details. Located IRS CP 575G letter (`~/Desktop/MyGrind Business/01 Legal/LLC Filings/EIN Letter (CP 575)_My Grind Sports LLC.pdf`), uploaded mid-day, 24h review window started. Verification expected by 2026-05-24. Means Phase 5 live-mode launch no longer gated on June 16 IRS database propagation. (2) EIN propagation pattern documented as new project memory `[ein_propagation_pattern]` — EIN was issued **2026-05-15** (per CP 575G letter, NOT 2026-05-19 LLC approval date which memory previously conflated). Earliest reliable third-party DB verification: 2026-06-05 to 2026-06-12. Twilio TFV still gated on this since no document upload escape exists there. (3) Resources panel rebuilt: moved Recruiting & Academic Resources card out of Settings into dedicated `panel-resources` accessible via More → 🎓 Resources. New Girls in Baseball section with 6 cards (WPBL, Baseball For All, USA Baseball Women's Team, MLB Develops, Hall of Fame Women in Baseball, LA Heat regional partner). Intro reframed from "smaller pathway" to growth narrative (WPBL launches August 2026, college club teams at Cal/UC Davis/USC/Washington/Occidental, NCAA Emerging Sport status as next push). Email Coach option removed (Decision #47 — doesn't scale at national volume). Typography bumped throughout panel for readability (20px Bebas Neue section heads, 11px → 13px badges, pro tip 13px gold-dark → 14.5px cream/light). 6 commits: `ece5ce8` `5a5a369` `5981a42` `a59dce8` `6feebce` + earlier-morning `8c4751d` (Firebase Analytics). SW cache v277 → v286 across 8 bumps. (4) .gitignore hardened (node_modules, .env*, .DS_Store, logs) + `package-lock.json` committed for reproducible Vercel builds. (5) Firebase Dynamic Links shutdown verified non-issue — magic-link auth already runs on custom Resend + Redis flow, zero `dynamicLink` references in codebase. (6) Pronoun audit on user-facing copy: 0 changes needed (Son/Daughter/Other dropdown already balanced). (7) Free-tier decision locked (#46) — stays paid-trial through launch. (8) 4 memory files updated: `llc_entity_details`, `twilio_tfv_state`, new `ein_propagation_pattern`, MEMORY.md index. (9) Conversation with Coach surfaced Sites family connection — `nancysites@` is already a MyGrind user since Apr 26, family founded LA Heat (Emma + Mia Sites). Strategic context now baked into product via LA Heat card. 3 Decisions locked (#46 #47 #48).

- **LAUNCH +4 (2026-05-22) — Support Tickets Filed + Coach Feedback Loop Closed + 7 Commits Polish Wave** — COMPLETE. (1) Twilio TFV Round 5 rejected AM with same Error 30527 (5th rejection); root cause confirmed as EIN propagation lag (EIN issued 5-19 has not yet hit Twilio's third-party verification database, typical 2-4 week lag). No field change can fix this. (2) Twilio support escalation filed via help.twilio.com → Submit a ticket with IRS CP 575 EIN letter, stamped CA Articles of Organization, and Certificate of Status attached. Expected human-reviewer response 24-48h. (3) Stripe 1099-K TIN switch ticket filed via support.stripe.com (carried from Day +2). Expected response 24-72h. (4) Marketing Plan v0 drafted in Notion at https://www.notion.so/368819955e6f8170a1e8f499c8176e54 — 8 sections, recommended sport #3: soccer, 3 open questions for Coach review. (5) Memory `twilio_tfv_state.md` rewritten with EIN-lag diagnosis and "DO NOT resubmit Round 6" instruction. (6) Vercel Analytics snapshot: 7-day window May 14-21 — 114 visitors (+78%), 967 page views (+118%), 32% bounce (-18%); funnel `/` 78 → `/signup.html` 56 (72% CTR) → `/softball.html` 30 → `/onboarding.html` 17 → `/signin.html` 8. (7) Seven code commits deployed (all READY): `fe37df5` top-right Sign In on landing page; `c8f05ae` Sign In size bump 15px/700; `0d5e717` self-funnel copy gaps on Screens 5/7/8 + title/meta; `6827f23` Coach Feedback MYFEEDBACK→MYCOACH reframing + honest SMS dry-run toast + Settings Recruiting & Academic Resources card (NCAA/NAIA/NJCAA/Perfect Game/PBR/Extra Inning Softball/NFCA/Khan/Common App/FAFSA/College Board); `9aeafc1` dropped "Talk to Coach" group from Resources card (Calendly was Coach-specific); `62bb780` root cause of "didn't make sense" fixed end-to-end — removed all `data-default` canned phrases in coach-reply.html, added validation (chip detail ≥15 chars OR comment ≥30 chars), player-side dashboard now shows tap-to-text follow-up CTA prefilled with original focus area; `7b9b130` "Why I Grind" 200-char profile field added under hero card + age-aware journal prompts that swap to kid-friendly language for `pl-age < 13`. SW cache v272 → v276 across 4 bumps.

- **LAUNCH +2 (2026-05-20) — Marketing Wave + Stripe LLC + Twilio R4** — COMPLETE. (1) Launch broadcast email sent to full waitlist (~56 contacts) from `coach@mygrindapp.com` — subject "MyGrind is live", preheader "35 years in the making. Be honest with me." — included sign-in-spam workaround block + share ask + FOUNDERMYGRIND CTA, drafted in Coach voice with no em-dashes. (2) Twilio TFV Round 4 filed inside prioritized review window after Round 3 was rejected overnight with new error 30527 (Business Registration Number Missing/Invalid) — root cause was Type set to BRN with CA Entity Number; Twilio US TFV only accepts EIN. Switched Type to EIN with value 42-2579197 + Issuing Country US. Review window expires ~2026-05-26. (3) Mailchimp audiences consolidated to one (was two) via export → import → delete; signup form was already wired to the correct surviving audience. (4) Stripe Public Business Name → My Grind Sports LLC. Customer Support → Legal business name swapped from "Michael Young" to "My Grind Sports LLC". Address Line 2 cleaned from orphan "191" → "Unit 191". Phone verification completed. Stripe Tax (sales tax automation) confirmed already enabled on all 9 payment links. (5) 1099-K TIN switch identified as Stripe-Support-ticket-only (locked at account creation, no self-serve UI). Ticket script drafted, filing queued for tomorrow AM. (6) 5-channel launch social pack drafted, 4 posted today (IG Feed + Story + Facebook + LinkedIn + X), TikTok deferred to evening. 13-post pack persisted in Notion at https://www.notion.so/366819955e6f81f884b6c9831dcf2b8d for ongoing reference. (7) Two code-cleanup commits deployed (see below).
- **LAUNCH +1 (2026-05-19) — Customer Recovery Stack** — COMPLETE. (1) `api/admin/signin-link.js` admin rescue endpoint shipped in commit `694e18c`. Coach hits `/api/admin/signin-link?email=X&token=ADMIN_RESCUE_TOKEN` in a browser, gets a working magic-link URL back, pastes into iMessage to any stuck customer. Bypasses Firebase email delivery entirely (which was sending sign-in emails to Gmail spam during launch week). Used to rescue first real launch-day customer (Aaron) on 2026-05-19 PM. (2) Send Setup Link modal on signup.html Screen 8 replaces the dead "Phase 3 placeholder" alert with 4 working paths: Copy Link, Send via Messages (navigator.share), Open on this device, Show QR Code. No backend dependency, no phone-number ask. (3) SMS opt-in consent text in the phone-confirmation modal strengthened with message frequency disclosure ("one per invite"), HELP keyword, inline Privacy Policy link, and explicit LLC brand-entity language.
- **softball.html legacy URL cleanup** — COMPLETE 2026-05-20 (commit `2383fb8`). Removed 7 hardcoded `youngsbaseball.github.io/mybaseballgrind` URLs from APP_URL/TRIAL_URL/SHARE_URL constants + 4 inline social captions. mg-config.js runtime override is now belt-and-suspenders rather than load-bearing. Also aligned softball.html `og:url` with its canonical (`www.mygrindapp.com`). SW cache v270 → v271.
- **Profile input auto-format polish** — COMPLETE 2026-05-20 (commit `561de18`). New `formatProperNoun()` helper auto-cleans player name + school/team inputs on blur. Catches `usc` → `USC`, `michael` → `Michael`, while respecting mixed-case input like `iLearn Academy` and `St. Mary's`. Wired to `pl-name` and `pl-school` via onblur. SW cache v271 → v272.
- **Vercel plan: Pro** — upgraded 2026-05-19 ($20/mo, personal card). 12-function Hobby cap eliminated. Function count now 15 (was hitting 12 cap yesterday). Enabled restoration of branded magic-link email endpoint, Stripe Customer Portal endpoint, Firebase Admin SDK lib (commit `5662ffe` reverted yesterday's hotfix `e65ea11`). `FIREBASE_ADMIN_SERVICE_ACCOUNT` env var live in Production + Preview (Sensitive). `ADMIN_RESCUE_TOKEN` env var live (48-char hex, Sensitive).
- **Legal entity** — My Grind Sports LLC APPROVED 2026-05-19 (1 day early). Entity No. B20260231794, EIN 42-2579197, Certificate of Status Cert No. 460108016. Statement of Information due 2026-08-13 (calendar reminder set 8-03). $800 CA Franchise Tax due 2026-09-15. All LLC docs filed in `/Users/baseballmike/Desktop/MyGrind Business/01 Legal/LLC Filings/`.
- **Phase 3c (Rate Limiting)** — COMPLETE + VERIFIED (Redis-backed, hashed IP/phone, fail-open)
- **Phase 3d (Twilio Lookup pre-check)** — COMPLETE (2026-05-02). `lib/lookup.js` runs Twilio Lookup v2 between E.164 normalization and rate-limit checks; rejects landlines, VoIP, and invalid numbers with friendly messages before any send budget is burned. Fail-open on Lookup outage.
- **Phase 4 — Player Onboarding** — `onboarding.html` shipped (per Decision #13: stripped of Stripe/Mailchimp/Firebase, just localStorage)
- **Phase 5 — Stripe wiring** — COMPLETE (2026-05-05). Step 1 (skip-trial Stripe redirect) shipped 2026-05-03. Steps 2-4 shipped 2026-05-05: webhook (`api/stripe-webhook.js`) verifies signatures and updates Redis subscription store on subscription events; Pay button on signup.html Screen 8 redirects to live Payment Links; softball.html syncs paid status from `/api/get-subscription` on every load. Stripe webhook destination "fascinating-oasis" live in Workbench listening to 5 events. Real subscriptions now flip `isPaid` automatically.
- **Phase 6 — Share + Settings buttons on signup dashboard** — COMPLETE (2026-05-02). Real modals replace the placeholder alerts. Share modal has copy-link + Web Share API + pre-written caption. Settings modal shows read-only account summary (name, email, phone, plan, players) + a support-email link for changes (self-serve editing lands when Phase 5 backend account management ships).
- **Family pricing tier** — COMPLETE (2026-05-02; price reconciled 2026-05-06 to match live Stripe link). $149.99/yr · $14.99/mo flat for 2-3 players in one household, alongside the existing Single tier ($99/yr · $9.99/mo per player). Anti-abuse stack: hard cap at 3 slots, slot-lock at first SMS-send, per-plan phone uniqueness, single-parent invite path (already in place).
- **Phase 7 — Player dashboard** — softball.html IS the live player dashboard. Walkthrough across five marathon sessions (2026-05-09 / 5-10 / 5-11 AM / 5-11 PM / 5-12): Daily Journal, My Stats, Profile, Goals, and Dashboard are now COMPLETE. Today's session (5-12, 21 commits, SW v145 → v167) shipped: Off Day flow built end-to-end (😴 dropdown label with "(full rest)", info banner, lock system blocking other training types when an off-day exists, hard duplicate guard in saveEntry, instant unlock on delete via renderAll wiring); Reflection redesigned with 🪞 intro card, mindset emoji picker (🔥 😌 🙏 😐 😤 😞 saved as entry.mindset), cream accent + Lora italic prompts, title no longer required; Reflection prompts opened up beyond today/tomorrow ("What's been on your mind?" / "What is this stretch teaching you?" / "What are you carrying forward?"); Off Day prompts rewritten to affirmation/recovery/family ("Today's affirmation", "How are you taking care of yourself?", "Who or what filled your cup today?"); Save Entry button feels like a button (:active press, gold pulse, haptic, universal pointerdown press-flash across every button); instant entry-list update on save (force `_entriesFilter` + renderEntries direct + last-resort fallback to 'all' filter); Dashboard reorganized for parent-awareness (Today's Plan removed as redundant, Weekly Recap + Parents Portal merged into single "Share With Family" card, Coach Comms lifted to top right under Academics, Hitting + Fielding grouped); Rest Days dashboard card (7d / month / season counters); streak emoji tiers (🧊 no streak / 🔥 active or 3+/week / 💎 streak ≥ 7); age band broadened to middle school (6th + 7th grade added to Profile, School/Team label inclusive of MS/HS/college/travel, MS-friendly goal presets including Make MS Team, JV, 60+/70+/75+ MPH pitching, Sub-8.0 / Sub-7.5 60-yard); modern font stack (SF Pro / Roboto / Inter for body, Bebas Neue + Barlow Condensed for branded display); touch accessibility (input font-size 15px → 16px to kill iOS auto-zoom, touch-action: manipulation to kill 300ms tap delay, transparent tap-highlight). Remaining walkthrough tabs: Settings + Coach Feedback inbox card on Dashboard.
- **Phase 7b V1 — Coach feedback loop** — COMPLETE (2026-05-04). End-to-end: player → SMS magic link → coach-reply.html → SMS player + parent dashboard card. Redis-backed `lib/feedback-store.js`, 4 API endpoints, 32-hex token auth, 90-day TTL. SMS via Twilio (DRY_RUN until TFV approves).
- **Phase 7b V1.5 — Parent weekly email digest** — COMPLETE in TEST MODE (2026-05-05). Vercel cron `/api/cron/weekly-digest` fires Mondays at 14:00 UTC (~7am PDT), scans Redis for active parent emails, sends warm-dark branded HTML digest via Resend. All emails redirected to youngsbaseball@gmail.com via `WEEKLY_DIGEST_TEST_EMAIL` env until `mygrindapp.com` is verified in Resend (DNS migration deferred). Sender: `onboarding@resend.dev`.
- **Adult self-signup funnel (Scope X)** — COMPLETE (2026-05-06). `signup.html` now opens with a Screen 0 audience picker ("My Athlete" / "Myself"). Self path branches via `state.signupFor='self'`: relabeled copy on Screens 1-4 (`applySelfLabels(n)`), playerCount question hidden on Screen 3, Screen 6 auto-fills + skips to Screen 7. URL handoff `?self=1` to `onboarding.html` triggers self-aware PIN screen + welcome + end-of-flow copy. Scope Y polish (Screen 8 dashboard + S14 deeper kid-coded copy) deferred.
- **Multi-sport platform Phase 1** — COMPLETE (2026-05-06). `softball.html` refactored into a sport-aware shell themed per `data-sport`. Central `SPORTS` config object (baseball/softball/both) with `getSportConfig()` helper. 165 hardcoded pink color refs → `var(--gold)` family. 16 hardcoded 🥎 → ⚾ HTML defaults (DOM walker swaps per sport). Bidirectional `applyBallEmoji()`. New `window.setActiveSport(s)` global re-themes live. Multi-sport toggle UI in dashboard header for `family=both` users (live ⚾↔🥎 swap, no reload). Recipe doc `docs/ADD_A_SPORT.md` makes adding football/volleyball/etc. a ~15-minute job. Phase 2 (sport-specific drill content) + Phase 3 (per-sport landing pages) deferred.
- **Promo code system** — COMPLETE (2026-05-06). Collapsible "Have a promo code?" gold-pilled button on Screen 5 of `signup.html`. Persists to `state.promoCode`. New `buildCheckoutUrl()` helper appends `prefilled_promo_code=` + `prefilled_email=` to Stripe Payment Link URL. Both checkout entry points (skip-trial + dashboard pay-early) use it.
- **Founder Launch Offer** — COMPLETE end-to-end (2026-05-13 stack, Mailchimp welcome live 2026-05-14). Full stack: Stripe coupon renamed "MyGrind Founders - 6 Month Free" with new promotion code `FOUNDERMYGRIND` (0/100, 6 months free); old `MYGRIND6` deactivated. `signup.html` PROMO_INFO swap + new `?promo=<code>` URL param handler in DOMContentLoaded so launch-day email links one-click into Screen 5 with code pre-applied and trial copy already flipped. `index.html` reframed as founder offer — new `.founder-note` block between hero and form with personal letter from Coach pitching FOUNDERMYGRIND, CTA swap ("Notify Me" → "Reserve My Spot", "Be First To Know" → "Reserve Your 6 Months Free"), trust line + success message rewritten in Coach's voice (re-edited 2026-05-14 to sign as "Coach" not "Coach Young" and drop em-dashes per Decisions #34 + #35). Mailchimp tag stays `2216797` — every signup from here is a founder cohort entry by definition. Stripe enforces the 100-redemption cap; email/page do not police (Decision #32). Mailchimp welcome automation rewritten in founder voice and LIVE 2026-05-14 (subject "Your founder spot is held.", sent immediately on subscribe from `coach@mygrindapp.com`). Launch-day broadcast email drafted, ready when Coach pulls the trigger.
- **Email infrastructure for mygrindapp.com** — COMPLETE (2026-05-13). SPF/DKIM/DMARC/PTR all valid. SPF: `v=spf1 +mx +a +ip4:192.64.118.103 +include:spf.web-hosting.com +include:spf.privateemail.com ~all`. DMARC: `v=DMARC1; p=none;` (monitor mode — tighten to quarantine after 30 days clean reports). Untangled the cPanel/Private Email shadow problem: MX has been pointing to `mx1/mx2.privateemail.com` all along (not `jellyfish.systems` as old memory note claimed). cPanel `coach@` and `support@` mailboxes were dead-letter boxes — could send outbound but never received inbound. Deleted the cPanel zombies. Private Email is now the single source of truth for `@mygrindapp.com` inbound (Decision #33). `coach@mygrindapp.com` Auto-Forward → `youngsbaseball@gmail.com` configured in Private Email webmail. `support@` forwarding queued (30-second click for Coach to repeat).
- **Brand voice + sole-prop legal posture** — LOCKED (2026-05-14). Decisions #34/#35/#36 formalized: universal "Coach" voice (never personal name), no em-dashes anywhere, sole proprietorship (no LLC). Cleanup commits: `253450e` voice rule across landing + coach-reply.html; `a38a683` em-dash purge from founder note + thank-you; `d7a9700` 8 LLC claim removals across privacy.html + terms.html. Twilio TFV resubmission unblocked once site + form became consistent.
- **Phase 7 walkthrough (Profile / Journal / Off Day / Dashboard / Calendar)** — 99% COMPLETE (was 95%, advanced 2026-05-14). Profile: generic School / Team placeholder, name field reworded, height/weight Notes example rewritten, 🎒 emoji dropped from Basics & Physical, ⚾ photo placeholder replaced with dark camera SVG, onboarding-to-softball name bridge bug fixed (`mg_player_profile` → `ybg_softball_profile` on first run). Journal: all expandables default collapsed, Game entry label "Title / Opponent" → "Opponent", Off Day prompt "Who or what filled your cup" → "Who was in your corner". Dashboard: Month Focus reads as standards not checkboxes, month card pulls live from `months[]` data (no longer stuck on April), D3 Academics tile added, Share With Family coordinates with all 7 entry types, My Photobook button removed (Pro feature), 12-Month Training Calendar tile surfaced, "📋 This Week" sample card pulls current week's prescription. Calendar: checkboxes converted to gold ▸ bullet rows, "← Back to Dashboard" pill added, 6 months realigned to real periodization (May/Jun/Jul/Aug/Nov/Dec). Only Settings tab + Coach Feedback inbox card walkthrough remain. SW cache v167 → v183 across 16 bumps.
- **12-Month Training Calendar — realigned to real periodization** — COMPLETE (2026-05-14). Web-verified against current CIF Southern Section (prelims May 14, finals May 29-30, state regionals June 2-6) and NCAA D1 (regular season ends ~May 24, CWS June 12-22) schedules. Six months rewritten: May (regular season closes + CIF playoffs path), June (post-season 3-week throwing rest + week 4 strength build kickoff), July (build phase continues, no longer duplicates rest content), August (escalates the build rather than introducing it), November (heavy lifting through Thanksgiving, transition begins Week 4 FRI/SAT), December (maintenance + yoga + explicit 2-3 week winter-break throwing rest). Calendar bullets converted from checkboxes to playbook-style gold arrow rows. Dashboard "📋 This Week" card samples the current week's prescription live. All copy passes em-dash brand-rule audit.
- **SEO foundation** — COMPLETE (2026-05-07 PM). Title rewritten "MyGrind — Baseball & Softball Training Journal App" (51 chars, brand + sport keywords). Description, Open Graph, Twitter Card all wired with explicit width/height/alt/type/locale. JSON-LD `@graph` on `index.html` with 3 nodes — Organization, WebSite, SoftwareApplication (with 3 Offer prices). 1200×630 OG card at `/assets/og-card.png` (Canva-generated, brand-kit aligned, LinkedIn preview verified). Vercel Web Analytics live + collecting on all 6 public pages (cookieless, privacy.html §11 compliant). Google Search Console URL-prefix property verified via `/google826fc67b2dde2efa.html`; sitemap submitted with **Success** status. Indexation timeline: 2-7 days first crawls, 2-4 weeks branded "MyGrind app" surfaces, 4-8 weeks full maturity. Single-reference doc lives at `docs/ANALYTICS.md`.
- **SEO Entity Authority** — ~95% COMPLETE (advanced massively 2026-05-16). Schema upgrade with new Person node + bidirectional founder ↔ worksFor link shipped (commits `508f2a4`, `639d87a`). Organization sameAs now includes linkedin.com/company/mygrind. Founder bio framing locked to "35 years in baseball as a coach, trainer, dad, and mentor" (replaces "20 years coaching"). Personal LinkedIn fully rebranded to MyGrind-primary at **linkedin.com/in/coachmikeyoung** (custom slug, banner + pic + name + headline + About + Founder position + Skills + Location all aligned, Open to Work off). YBG LinkedIn Company Page DEACTIVATED (14-day soft delete ends ~2026-05-30). MyGrind LinkedIn Company Page location set Santa Clarita CA. Crunchbase profile SUBMITTED (manual review, 1-5 business day SLA, lands at crunchbase.com/organization/mygrind). First MyGrind-branded LinkedIn post live. Remaining 5%: add Crunchbase + GBP URLs to sameAs once approvals land, monitor GSC recrawl. Knowledge Graph entity disambiguation should mature 5-7 days from today. Strategy locked per Decision #39: optimize for "MyGrind" (one word) only; concede "my grind app" two-word query to Grindr permanently.

- **Pre-launch app polish marathon** — 12 commits 2026-05-16 (`0e4723a` → `770b69c`). More menu rebuilt (Training Calendar removed, Help & FAQ modal added, Send Feedback + Suggest Feature mailto buttons added, My Team + Coach Feedback hidden for August 2026 coach-side sprint). Back-to-Dashboard pills added to Team / Coach Feedback / Settings panels. Bottom nav reordered Dashboard-first (matches Strava / MyFitnessPal / Whoop / Apple Fitness+ / Hudl / Notion pattern). "Baseball Journal" → "Sports Journal" → "Players Journal" rebrand wave (sport-neutral, future-proofs new sports). "No Grades, No Baseball" → "No Grades, No Game" across 13 instances. Coach Young personal-name attributions purged from both quote arrays (Decision #34) and replaced with "The Grind". Edit Mode banner upgraded to hero block (Bebas Neue headline + gold gradient + explanatory body copy). Quote arrays doubled from ~30 to 70 entries each (10 weeks unique daily content). Em-dash sweep first pass cleared 28 high-visibility instances (421 remain in deeper surfaces, queued). Coach notes get a new "📤 Send to Coach" share button on past entries (OS share sheet, workaround for hidden Coach Feedback panel). SW cache v183 → v194 across 11 bumps. Only Lucide icon swap remains before player-side launch-ready.

- **Pre-launch walkthrough polish + Pro gating + nav-drift closure** — 13 commits 2026-05-16 (`fcc25f6` → `c5158fc`). Player-side walkthrough polish: Profile Bats/Throws expanded to all 6 combos (R/L + S/L added — bats right throws left was missing), placeholder text dimmed to 38% opacity so example copy reads as hint not user input, Dashboard Coach's Note 13→15px, iOS QuickType autofill bar (passwords / credit cards / contacts / locations) suppressed on non-credential inputs via autocomplete=off + autocorrect=off + autocapitalize=off + spellcheck=false on every input without an explicit autocomplete value. Custom pull-to-refresh added then removed end-to-end (Decision #41 — MyGrind is local-first, nothing to pull from a server, touch handler was interfering with iOS scroll). Main padding-bottom journey 100→140→220px after Email Week + Full Journal buttons got visibly clipped by the fixed nav (root cause: main uses `height:100%` of body which physically extends ~64px below viewport because header takes that space at the top in normal flow — that swallows 64px of any padding-bottom; architectural fix to `height:calc(100% - header)` deferred to post-launch). iOS bottom-nav drift hunt closed across v2/v3/v4 attempts — real fix was switching body from `100dvh` to `100svh` (dynamic viewport height resizes with URL bar transitions, small viewport height is locked so the fixed nav has nothing to chase); overscroll-behavior briefly bumped to `none` for extra safety but reverted to `contain` after the strict form triggered an iOS Safari edge case that locked inner scroll on the Profile panel. Academics core-course alerts built (per-slot NCAA keyword check across English/Math/Science/Social/Language with soft amber warning when class name doesn't match the slot's expected category, per-slot 'Mark as elective' override for seniors who finished a core requirement early, core-aligned bottom summary) then gated entirely behind `const MG_PRO = false` flag (Decision #40 — keyword warnings on every wrong class felt like homework nagging that risked discouraging casual players; the prescriptive layer ships with MyGrind Pro Phase 2B alongside the full year-by-year tracker). Free tier now shows ONE soft 'Coming with Pro · August 2026' tease tile instead of six per-slot warnings. Hotfix mid-session: duplicate `summary` var/const declaration in renderCoreAlerts crashed the main script block on parse, leaving Coach stuck on the splash screen — renamed to `summaryHide`. Bad quote pulled from both rotation arrays ('Your jersey number changes. Your work ethic doesn't.' — logically backwards because work ethic IS the thing a player controls; pool 140→138). 3 Decisions locked (#40, #41, #42). SW cache v212 → v225 across 13 bumps. ROADMAP additions: full quote pool athletic-truthiness audit (Decision #42, 60-90 min).\n\n- **Mobile UX Marathon + Lucide Phase 1-2 + App Shell** — 20 commits 2026-05-17 (`ed8ebee` → `b5fe193`). Lucide icon system Phase 1 infrastructure (26-icon SVG sprite + `mgIcon(name, opts)` helper, zero visual change) and Phase 2a (bottom nav tabs swapped: home / book-open / bar-chart-3 / user — More tab keeps ⚾ as cultural icon). iOS Safari bottom-nav drift fully resolved via APP-SHELL SCROLL ARCHITECTURE: `html, body { height: 100dvh; overflow: hidden; }` + `main` is the scroll container with `-webkit-overflow-scrolling: touch`. Body can't scroll so the fixed nav anchors to a viewport that physically cannot shift. Same pattern Strava / Notion / Apple Fitness+ use. Took 4 fix iterations before landing the nuclear app-shell solution. Tab order swapped: Dashboard / Journal / Profile / Stats / More (Profile to slot 3). Back-button hash navigation: `mgTab()` pushes hash state on every tab switch, popstate listener handles back/forward. Players navigate in-app history before exiting. URLs become shareable (`#stats`) and refresh-stable. UX polish: input labels global 11→15px + reflection 14→16px, practice-mode chips dropped emojis + 14→16px, custom gold chevrons on all `<select>` dropdowns (default browser arrows were invisible on dark theme), DNP·Did Not Play chip added to game grade row, Middle School consolidation (6th/7th/8th merged), Profile H/W check-in form progressive disclosure (collapsed by default, auto-expand when overdue, × close button, button renamed to "Update", scroll-to-top + success toast on save, 5 small section labels bumped 10-11px → 12-14px), Academics class-name-before-grade gate (no orphan grades, disabled state visually obvious at 45% opacity). Also Academics Phase 2A Summer Mode (commit `e74c6f0`, late-Saturday catch-up) — Dashboard card auto-detects June-Aug, locks GPA tiles into "Spring [Year] Final Grades · Locked" treatment. ROADMAP entries added: Phase 2A/2B split + Pro-tier scope (Coach dashboard + Ask Coach AI chatbot). SW cache v194 → v212 across 14 bumps.
- **MyGrind legal entity** — FILED 2026-05-15. My Grind Sports LLC submitted to CA Secretary of State (approval check 2026-05-20 at bizfile.sos.ca.gov). EIN 42-2579197 issued by IRS. DBA: MyGrindapp. Separates MyGrind from Young's Baseball Group (still sole prop) for compliance and liability. CA calendar deadlines: 2026-08-05 Statement of Information ($20), 2026-09-15 Franchise Tax ($800). Coach is a 70% VA-disability veteran; DVBE (CA state) + SDVOSB (federal) certifications eligible after LLC approval.
- **Email aliases on @mygrindapp.com** — LIVE 2026-05-15. Four aliases wired through Namecheap Private Email all routing into coach@'s mailbox: hello@, press@, billing@, support@. Test email from hartdistrict.org → support@mygrindapp.com landed in Gmail at 6:14am PT confirming end-to-end forwarding. Unified auto-reply ("Got your note.") drafted for Vacation Notice paste in Private Email webmail.
- **Business Plan** — 14/14 sections COMPLETE
- **48 Locked Decisions** (3 added 2026-05-23: #46 Pricing model stays paid-trial through launch — don't add free tier until 90 days post-launch with under 50 paid OR conversion under 15% OR 200 paid users with stalled growth, whichever first; #47 No direct email contact promoted in-app at national scale — Resources/Settings/Help route contact through structured Send Feedback form, FAQ self-serve, email addresses stay in Privacy + Terms only; #48 Girls baseball is a first-class audience in MyGrind — dedicated Resources section, national orgs lead/regional follows, growth narrative not apology; 3 added 2026-05-21: #43 one brand/site/app for all sports, #44 national from day 1, #45 sport advisor per new sport; 3 added 2026-05-16: #40 Academic prescriptive layer is Pro-tier not free — keyword warnings + per-slot elective override + core-aligned summary gated behind MG_PRO flag so the free tier stays accountability-focused; #41 Pull-to-refresh is not a MyGrind pattern — local-first app has nothing to pull from server, SW auto-updates handle deploy refresh, custom PTR removed; #42 Every quote in the pool must pass athletic-truthiness audit — locus-of-control + age-appropriate + brand-voice rules apply to all 138 entries across YBG_QUOTES + YBG_QOTD; 2 added 2026-05-15: #38 form My Grind Sports LLC in California to solve Twilio TFV legal entity verification and separate MyGrind from YBG, #39 optimize brand SEO for "MyGrind" one word only and concede "my grind app" two-word query to Grindr permanently; 4 added 2026-05-14: #34 universal "Coach" voice for MyGrind never personal name, #35 no em-dashes or AI-pattern punctuation in any MyGrind/YBG copy, #36 Young's Baseball Group is a sole proprietorship not an LLC, #37 12-month calendar is reference playbook not checklist; 3 added 2026-05-13: #31 founder-letter voice for launch messaging, #32 Stripe enforces the 100-redemption cap not the page/email, #33 Private Email is sole source of truth for @mygrindapp.com inbound; 3 added 2026-05-11: #28 Off Day is a first-class entry type distinct from Reflection, #29 every entry is tagged with the sport it was logged under, #30 streak counts every entry type including Off Day; 3 added 2026-05-10: #25 edit-PIN protection on entries >24h old, #26 solo practice is a first-class entry sub-type, #27 two-way coach messaging deferred to Phase 7b V2 / MyGrind Pro; #24 implicit from 2026-05-09 marathon — workout entry type, journal-sourced stats, fielding stats, derived game-day arm load; #23 from 2026-05-07: onboarding completion auto-grants softball.html trial access; #20-22 from 2026-05-06: multi-sport architecture, Family Annual = $149.99, self-signup is a branch in signup.html) — source of truth in Notion

## ✅ Features Shipped (`softball.html` — the journal app)

### Core
- [x] Daily Journal — log games, practices, reflections, instructor sessions
- [x] Game stats with auto-calculated AVG / OBP / SLG / ERA
- [x] Practice rep counter (swings, ground balls, throws, pitches)
- [x] My Stats season totals (auto-populated from journal)
- [x] Goals tracker (3 measurable goals)
- [x] 12-month Training Calendar with weekly prescriptions
- [x] Academics (5 core subjects + custom classes)
- [x] Profile (positions, height, weight, photo, sport)
- [x] Team panel (coaches/players)
- [x] Streak tracking + milestone celebrations
- [x] Daily inspirational quote on Dashboard
- [x] Coach/Parent comment logging on entries
- [x] Daily affirmation banner (60 affirmations, rotates by day-of-year)
- [x] PWA install prompt + offline support
- [x] **Backup & Restore** — JSON export/import of all `ybg_*` data in Settings tab (re-shipped 2026-05-02)
- [x] **Backup nudge** — non-blocking banner appears when user has 3+ entries and no backup in 7+ days (re-shipped 2026-05-02)

### Cloud Sync (Firebase)
- [x] Sign in with Google (Firebase Auth)
- [x] Manual "Save to Cloud" / "Restore from Cloud" buttons
- [x] Cross-device data recovery
- [x] Per-user privacy via Firestore rules

### Sport Theming
- [x] Sport picker on trial signup (Baseball / Softball / Both)
- [x] Pink palette + softball quotes for softball mode
- [x] 20 softball-specific quotes
- [x] Universal "NO GRADES · NO GAME" banner (sport-neutral)

### Trial / Subscription
- [x] 7-day free trial (auto-starts on signup)
- [x] Email-based access
- [x] Stripe integration (live `buy.stripe.com` URLs hardcoded in `softball.html`)
- [x] Team Coach plan ($29.99/yr) and Team Sponsor plan ($300/yr) options

### Tabs (in order)
1. ⚾ Dashboard
2. 📓 Daily Journal
3. 📊 My Stats (now includes Training Volume + arm-load tier card)
4. 🏆 Goals (lives on Dashboard now — old standalone tab removed earlier)
5. 📚 Academics
6. 📅 Training Calendar
7. 👥 Team
8. 👤 My Profile
9. ⚙️ Settings (now includes Backup & Restore)
- ~~📲 Share~~ — REMOVED 2026-05-08. Share modal still works for milestones; the dedicated Share tab and panel-share div were retired (panel kept hidden in DOM for future restore, More-menu entry deleted).

### Active beta users
- ~7 (per last count — verify in Firebase Auth dashboard if you need a fresh number)

---

## 🐛 Known Issues / Followups

*Add discovered bugs to `docs/ROADMAP.md`. This list is for STABLE issues we've decided to defer.*

- ⚠️ **Two-host situation** — `mygrindapp.com` is the primary, `youngsbaseball.github.io/mybaseballgrind/` is legacy. `softball.html` still has hardcoded github.io URLs that `mg-config.js` overrides at runtime. When changing URLs, grep both.
- ⚠️ "Hover" issue on Daily Journal top dropdown reported — needs clarification (no specifics yet)
- ⚠️ Softball image showing where a baseball should — needs screenshot to identify
- ⚠️ Duplicate `escapeHtml()` function in `signup.html` (defined at lines 3264 + 3417) — second wins, both nearly identical. Maintenance trap.
- ⚠️ SMS preview in `signup.html` (line ~3344) shows `mygrind.app/start/abc123` — fake URL with placeholder token. Real Twilio send (when 3b unblocks) will generate a different link, but the preview is misleading to parents.
<!-- Share + Settings buttons fixed 2026-05-02 (Phase 6 shipped) — alerts replaced with real modals. -->

---

## 📈 Metrics to Track (when you're ready)

- Number of waitlist signups (Mailchimp dashboard, audience `73280b4c02e9bc56c7e633892`)
- UTM source breakdown (which channel converted best)
- Number of active app users (Firebase Auth dashboard)
- Number of daily journal entries created (Firestore queries)
- Backup adoption % (count of `ybg_last_backup` keys via cloud-sync if/when we wire that)
- Trial → paid conversion rate (Stripe dashboard)

**Vercel Web Analytics** is now live (2026-05-07) — see `docs/ANALYTICS.md` for the full reference doc covering visitors, free trials, paid subscribers, and Search Console indexation. Weekly + monthly review checklists included.
