# 🛣️ My Grind — Roadmap

*Re-prioritize this file at the start of every week. Move shipped items to the bottom and into STATUS.md.*

---

## 🔥 This Week (pick 1 per session)

### P0 — Day +3 queue (added 2026-05-20 PM)
1. **File Stripe Support ticket to switch 1099-K TIN from SSN to EIN.** Self-serve UI doesn't expose tax classification change for established accounts. Ticket script drafted: quote Account ID `acct_1TOIr4Pm4ermqky4`, request Sole Prop → Single-Member LLC (disregarded entity) with EIN 42-2579197. Attach Articles + Cert + EIN letter if Stripe asks. 24-72h to resolve. NOT URGENT — 1099-K isn't issued until January 2027. ~5 min to file.
2. **Open the LLC business bank account** (carried from 5-19 queue). All required docs filed in `~/Desktop/MyGrind Business/01 Legal/LLC Filings/`. Print 2 copies of Articles + Cert + EIN + photo ID, walk into Chase / BofA / credit union. ~30 min at the branch. After: update Stripe payout destination in `dashboard.stripe.com/settings/payouts`.
3. **TikTok film + post** if not done last night. 22-second script + on-screen text overlays in Notion launch page Batch 1. Update @youngsbaseball TikTok bio link to `mygrindapp.com/signup.html?promo=FOUNDERMYGRIND` BEFORE posting.
4. **Monitor launch performance** (5 min daily):
   - Mailchimp open/click breakdown (stabilizes ~24h post-send, ~Thu 9am)
   - Vercel Analytics traffic continuation
   - Firebase Auth user count — if it ticks from 4 → 10+, the spam-workaround broadcast worked
   - `coach@mygrindapp.com` for rescue requests + feedback

<!-- SHIPPED 2026-05-20:
- Launch broadcast email to full waitlist (~56) — "MyGrind is live" sent from coach@mygrindapp.com mid-morning. Done.
- Stripe business profile update — Public Business Name → My Grind Sports LLC, Customer Support legal name updated, Address Line 2 cleaned, phone verified. 90% done; the remaining 10% (1099-K TIN switch) requires Stripe Support ticket and is moved to today's #1 above.
- Twilio TFV Round 4 — filed inside 7-day prioritized window after Round 3 rejected for error 30527. Switched Registration Type BRN → EIN, value to 42-2579197.
- Mailchimp audience consolidation — merged to single audience (`73280b4c02e9bc56c7e633892`) and renamed.
- softball.html legacy github.io URLs purged (commit `2383fb8`).
- Profile input auto-format polish on blur (commit `561de18`).
- 5-channel launch social pack — IG Feed + Story + Facebook + LinkedIn + X posted; TikTok script ready, deferred to evening.
- Launch social posts Notion page created — https://www.notion.so/366819955e6f81f884b6c9831dcf2b8d (13 posts across 6 channels with checkboxes).
-->

### P0 — abfrianela email-mismatch bug (added 2026-05-20 PM, backlog)
**Symptom:** Settings shows different Firebase Auth email than localStorage profile (e.g. Coach's device showed `abfrianela@gmail.com` as Registered Email but his own Michael profile data in Journal). Happens when a different magic link is tapped on a device that already has another user's profile cached.

**Workaround for any affected customer:** Sign Out → Safari Settings → Clear Website Data for mygrindapp.com → sign back in with their actual email.

**Code fix (~30 min):** Detect Firebase Auth `currentUser.email` change vs cached `profile.email` mismatch on softball.html load. Either (a) reset localStorage on identity change, or (b) prompt user "you signed in as X but the journal is for Y — switch or merge?" Files: softball.html, around the cloud-sync prompt area.

### P0 — stale "14 days free, no card" share-caption copy (added 2026-05-20 PM, blocked on offer decision)
4 instances in `softball.html` SHARE_CAPTIONS + 4 in `mg-config.js`. The offer is now founder-rate (6 months free via FOUNDERMYGRIND) for the first 100, not a 14-day trial. Cannot sweep until Coach decides new copy: Option A "Try MyGrind free" (generic, true for all), Option B "Founder rate is open" (only true while 100-cap holds), or Option C explicit "6 months free for founders, then $99/yr". Decide first, sweep after. ~15 min after decision.

<!-- SHIPPED 2026-05-19 (was P0 above):
- Real-customer testing pass on launch-day fixes: COMPLETE. All 4 paths verified (FOREVERYOUNG2026, FOUNDERMYGRIND, default 14-day, onboarding-loop fix).
- Endpoint consolidation for branded email + Stripe portal: REPLACED with Vercel Pro upgrade ($20/mo). 12-function cap eliminated, hotfix `e65ea11` reverted in commit `5662ffe`. Both endpoints restored without consolidation. Branded email still gated on Resend domain verification.
-->

### P0 — Cross-cohort safety (added 2026-05-19 PM)
4. **`/api/admin/signin-link` admin rescue endpoint.** SHIPPED in commit `694e18c`. Coach hits `https://www.mygrindapp.com/api/admin/signin-link?email=X&token=ADMIN_RESCUE_TOKEN` in browser, gets a working magic-link URL via Firebase Admin SDK, pastes into iMessage to any stuck customer. Token in 1Password. Use whenever a customer can't sign back in regardless of cause.
5. **Send Setup Link modal on signup.html Screen 8.** SHIPPED in commit `aeb4038`. Replaces dead Phase 3 placeholder alert with 4-path share modal (Copy Link, Send via Messages, Open on this device, QR Code). No backend dependency, no phone-number ask.
6. **SMS opt-in consent strengthened.** SHIPPED in commit `d0e1df1`. Added HELP keyword, message frequency disclosure, inline Privacy Policy link. Clears Twilio TFV Errors 30506/30513.

### P0 — Polish leftover from the audit
1. **Fix duplicate `escapeHtml()` in `signup.html`** — defined at lines 3264 + 3417. Consolidate to one definition. ~5 min.
2. **Fix the SMS preview URL in `signup.html`** — `buildSmsForPlayer()` (line ~3344) hardcodes `mygrind.app/start/abc123`. Should use `mygrindapp.com/onboarding.html?name=...` so the preview matches what Twilio will actually send when Phase 3b unblocks. ~10 min.

### P0 — Subscription / cancellation policy (added 2026-05-11)
3. **Stripe webhook → parent cancellation email.** When `customer.subscription.deleted` (or `customer.subscription.updated` with `cancel_at_period_end=true`) fires, send a confirmation email to the billing email on file via Resend. Body: "Your MyGrind subscription has been canceled. You keep access through {period_end_date}. Your player's journal will be held for 90 days — re-subscribe within that window to restore everything." Update `api/stripe-webhook.js` to handle these events; pull billing email from the Stripe `customer` object. The user-facing policy is already live in Settings (commit `717fa44`) — without this backend hookup, the parent-notification promise doesn't fire. ~1-2 hr.
4. **90-day retention purge job.** Scheduled Vercel cron (or Firebase Cloud Function) that runs daily, finds accounts canceled >90 days ago, and permanently deletes: Redis subscription record, Firebase `entries` / `goals` / `profile` collections for that uid, and any Resend audience entry. Add a `canceled_at` timestamp to the Redis subscription record at cancellation time so the purge job has a reliable signal. Surface "Account permanently deleted on {date}" email at the 90-day mark. The user-facing policy promises this in Settings — backend job has to honor it. ~3-4 hr including testing.

<!-- Phase 6 placeholder buttons fixed 2026-05-02 — Share + Settings now open real modals. -->

### P1 — Real-user walks (per CLAUDE.md verification protocol)
4. **Walk every screen of `signup.html` on phone width** — confirm progress dots, validation, back-nav, and SMS preview all behave end-to-end after recent edits.
5. **Walk every screen of `onboarding.html` on phone width** — verify the CSS-corruption fix from 2026-05-02 actually restored sticky progress bar + min-height layout. Especially S0 → S05 (PIN) → S1 → S14 path.
6. **Investigate "hover" issue** — user reported can't hover at top of something on Daily Journal. Need screenshot. Then fix. ~15 min.
7. **Investigate "softball image where baseball should be"** — need screenshot. ~15 min.
8. **Title fallback for Instructor entries** — currently saves as "Entry" if title is hidden. Should auto-fill from instructor name + focus. ~10 min.

---

## 📅 Next 2 Weeks

<!-- Phase 5 SHIPPED 2026-05-05. See "Recently Shipped" below. -->
<!-- Phase 6 SHIPPED 2026-05-02. See "Recently Shipped" below. -->

### P2 — Phase 5 follow-up: full account-edit Settings
Phase 5 backend is now in place. Upgrade the Settings modal from read-only to support inline email/phone updates and a Stripe billing-portal link. Currently the modal points users at support@mygrindapp.com for changes.

### P3 — DNS migration so the digest can email real parents
Move nameservers from Namecheap Web Hosting DNS to Cloudflare → verify `mygrindapp.com` in Resend → set `RESEND_FROM=MyGrind <notifications@mygrindapp.com>` in Vercel → delete `WEEKLY_DIGEST_TEST_EMAIL`. ~1 hr active + 24-48hr propagation. Risk: must capture all current records before changing nameservers, or website + `@mygrindapp.com` mail goes down. Current records to preserve (verified 2026-05-13): A → 216.198.79.1 (Vercel apex); MX → mx1.privateemail.com + mx2.privateemail.com (priority 10); TXT SPF → `v=spf1 +mx +a +ip4:192.64.118.103 +include:spf.web-hosting.com +include:spf.privateemail.com ~all`; TXT DKIM at `default._domainkey`; TXT DMARC at `_dmarc` → `v=DMARC1; p=none;`. When verifying with Resend, the SPF will need to grow to `include:_spf.resend.com` and a new Resend DKIM TXT record will need to land.

### P5 — Phase 7 (Player dashboard)
`onboarding.html` S14 currently shows a holding-screen overlay. Phase 7 is the real player dashboard — likely a stripped-down `softball.html` for the kid side or a new `player-dashboard.html`.

### P6 — Real-time Cloud Sync (Phase B v3)
Third attempt at auto-sync. The first two attempts caused full UI breakage in `softball.html`. **Do this only after P1 done AND with a clear test plan.** Key lesson: don't add a full-screen overlay that can become unclickable. Use a non-blocking pattern.

### P7 — Discoverability ("Why isn't anyone using this?")
If beta users aren't logging entries daily:
- First-run tour (3 quick screens explaining the app)
- Simpler dashboard for new users ("start here" card)
- Clearer call-to-action on the trial signup

---

## 🌅 Future / Maybe

- Push notifications (real lock-screen pushes — requires Firebase Cloud Functions, $25/mo)
- Multi-player accounts (one parent buys, multiple kids use)
- **Coach-facing section / dashboard — Phase 7b V2 (MyGrind Pro, August 2026 target)** — Coach Young 2026-05-16 walkthrough decision: launch the player app first, build the coach side once schools restart in August. Per Decision #27, the coach dashboard is the paid Pro tier (free tier keeps the 1-to-1 SMS feedback loop from Phase 7b V1). Scope: a coach-facing area inside softball.html (or a separate coach.html) where a coach signed in with their email sees a roster of all their players, recent entries summary, ability to send/receive feedback at scale (not 1-by-1 SMS), and team-level stats dashboard. Phase 7b V1 (player → coach SMS via coach-reply.html) already shipped 2026-05-04 — Phase 7b V2 is the Pro-tier dashboard layer on top. Lucide icon swap (above) should land BEFORE this work so the coach view uses the polished icon system from day one.
- **Team management section (August 2026 target)** — Coach Young 2026-05-16 walkthrough decision: hidden from More menu for launch, panel still in DOM (`#panel-team` in softball.html around line 4088). Currently shows a "join a team or create one as a coach" hero card but the join/create/invite flow isn't fully wired to a backend. Re-enable the My Team button in the More menu (currently commented out around line 2025) once the flow is complete and the coach-facing roster view exists. Belongs in the same August sprint as the coach-facing section above.
- **Parent invite — alternative paths at signup (post-launch P1)** — Coach Young 2026-05-14 walkthrough. Current signup assumes the player has a phone for the SMS invite. Many 8-12 year olds do not. Add two alternative paths on Screen 6: (a) "I'll set up MyGrind on this device right now" — skips SMS entirely and routes the parent's device straight to onboarding (best UX for the parent-and-kid-in-same-room case); (b) "Send the link to my email instead" — Resend-backed email invite the parent opens on whatever device the player will use. Option (a) is ~1-2 hr (UI toggle + route handling). Option (b) is ~3-4 hr (UI + Resend backend + email template). Ship (a) first, (b) later if real users ask.
- **Parent weekly digest — opt-out mechanism (post-launch P1)** — Coach Young 2026-05-14 walkthrough. Current architecture: `api/cron/weekly-digest.js` fires every Monday and auto-sends to every parent email with feedback activity in the past 7 days. Player can also manually share anytime via the Share With Family card. What is missing: a parent-side opt-out. Add a One-click "Unsubscribe from weekly updates" link in every digest email that hits `/api/unsubscribe?token=...`, sets a Redis flag `feedback:digest:optout:<email>`, and serves a simple confirmation page. Update the cron loop to skip emails whose flag is set. ~1-2 hr. CAN-SPAM compliance also benefits from this. The digest can stay opt-out (auto-enrolled) per Coach's intent.
- **Photo + video on entries (MyGrind Pro)** — Firebase Storage backed, cross-device, accessible to coaches inside the Pro coach portal. Live-app UI pulled 2026-05-11 because localStorage stripped media dataUrls on save (5MB cap) and photos vanished on reload. JS hooks left dormant in `softball.html` (handleFileSelect / processFiles / pendingMedia / renderMediaPreview) — wire to new affordance + Firebase Storage upload when Pro lands.
- **My Photobook (MyGrind Pro)** — dashboard entry button removed 2026-05-14 walkthrough. `openPhotobook()` aggregates every photo across all entries into a single grid view, but suffers the same localStorage-strip problem as inline photos above. Re-surface the entry button when the cloud-backed media system in MyGrind Pro lands (Phase 7b V2). Function + `#photobook-overlay` modal are still in code; just no UI entry point.
- **App-wide icon system — Lucide SVGs replace system emojis** — Coach Young 2026-05-14 walkthrough. The current app uses 30+ different system emojis as UI chrome (🏋️ 🤸 📋 📓 📚 💪 📷 🏆 etc.). Cross-platform rendering is inconsistent (iOS color, Android flatter, browser fallbacks), sizes drift across surfaces, and the colorful cartoon language clashes with the dark + gold premium brand. Top sports/training apps (Strava, Whoop, Nike Training Club, Apple Fitness+, Hudl, Linear, Notion) all use custom monochrome SVG icons in their brand color. Plan: add Lucide SVG sprite/helper, build `mgIcon(name, size)` function, replace utility emojis surface-by-surface starting with Dashboard. Keep emojis only where they carry cultural meaning: ⚾/🥎 (sport identifier), 🔥/💎/🧊 (streak tiers), 🦃/🎄 (seasonal). Scope: ~4-5 hr focused session. Best done with phone-side-by-side visual review.
- **Calendar — nutrition / recovery / busy-player tips per month** — Coach Young 2026-05-14 walkthrough. The 12-month calendar (`months[]` array around line 5227 in softball.html) already has `name`, `type`, `focus`, `intro`, `weeks`, and `note` per month, all in Coach's voice. Add three new fields: `nutrition` (eating like an athlete, month-specific — e.g. off-season needs more fuel for heavy lifting, in-season needs faster-recovery foods), `recovery` (sleep / mobility / arm care emphasis for the month), and `busyTips` (the journal version of "I had no time to log today — what's the 30-second version?"). Render in the expanded month card next to the weeks content. Voice has to be Coach's, not generic athlete-nutrition copy. Approach: draft structure first, write Jan + Jun together as samples, then batch the other 10 with Coach reviewing. ~30-45 min focused session.
- **Strength progress tracking for workouts (MyGrind Pro)** — per-exercise start weight + current max + pounds increased over time. Each workout entry's lifts get parsed (e.g. "Trap Bar DL 3x5 @ 225") so the app surfaces a strength-progression card showing PR history, plateau alerts, and "+X lbs since you started" callouts. Belongs in Pro because it needs cross-session aggregation, charts, and a structured lift-log schema (vs. today's freeform textarea). Coach Young noted 2026-05-11 during the workout-entry walkthrough.

- **"Ask Coach" AI chatbot (MyGrind Pro)** — Coach Young 2026-05-16 strategic conversation: AI trained on Coach Mike's 35 years of player development knowledge + the player's own journal data, providing personalized training advice. The player asks a question ("my swing is rolling over on outside pitches, what should I work on?") and the AI responds in Coach's voice, referencing the player's recent entries and goals. Premium MyGrind Pro feature, NOT a help-navigation bot — different product, different conversation. Backend: Vercel serverless function calling Claude Haiku or GPT-4o-mini API (~$0.002/query, ~$30-300/mo at 100-1000 daily users). Free tier keeps the existing static FAQ + mailto channels. Build trigger: only after Pro tier has paying users — economics don't work pre-revenue. Could anchor the Pro upgrade pitch entirely on its own ("Pro unlocks Ask Coach"). Source conversation captured in eod-sync log 2026-05-16.
- **Academics — Summer Mode (free tier, August 2026 target)** — Coach Young 2026-05-16 walkthrough surfaced the gap: current academics card assumes school is in session year-round and goes stale in June/July when no new grades arrive. Phase 2A (free-tier summer mode) transforms the Dashboard academics card in June-August: (1) **freeze the prior semester GPA + eligibility badge** in a "📚 Spring [Year] Final Grades · Locked" treatment (read-only, prevents stale-data drift); (2) **optional summer course tracker** — small input form that only appears if player taps "I'm taking summer classes", lets them log course + grade as they go; (3) **AP/honors prep checklist** for fall course readiness; (4) **ACT/SAT date countdown** for sophomores+; (5) **junior-year NCAA core-grade lock countdown** for upperclassmen; (6) **calendar academic tips swap to summer-coded copy** (reading, online courses, college prep) for June/July/August months. Auto-detect "is it summer" by current date (June-August = summer mode). When August arrives, card unlocks for fresh school year input. Build target: August sprint alongside coach-facing dashboard. ~2-3 hr focused work. Brand voice rules apply — no em-dashes, sport-neutral copy, Coach's direct voice.

- **Academics — NCAA Core-Course Tracker + advanced GPA (MyGrind Pro)** — Phase 1 (basic 6-subject view + GPA-based eligibility badges on Dashboard + Profile) shipped 2026-05-11. Phase 2A (summer mode) lands free tier in August 2026 per entry above. Phase 2B is the deep NCAA stack, locked to Pro per Coach Young: (1) full 16-core-course tracker across 4 HS years (4 yr English / 3 yr Math / 2 yr Science / 2 yr Social Studies / 4 yr additional / 1 extra Eng-Math-Sci), with year-by-year completion checkboxes; (2) NCAA-accurate core-course GPA calculation (different from school GPA — only specific classes count, scaled with honors/AP weighting); (3) junior-year lock-in alert (NCAA locks 7 of 16 core grades after junior year — Pro pings the player + parent before the deadline); (4) **dedicated Academics tab** (Coach Young 2026-05-11) — elevate from card-on-Dashboard to a top-level tab when Phase 2B features land. Card stays for the free-tier journal flow, tab unlocks under Pro for the depth; (5) **school-year-aware framework** (Coach Young 2026-05-16) — player enters their school start/end dates in Profile, app auto-detects school-year vs summer, GPA carries forward between years, Profile gets an Academic History view of past years. Source: NCAA Eligibility Center fact sheet + NCSA core-course guide.
- **Quote pool athletic-truthiness audit (post-launch P1)** — Coach Young 2026-05-16. Decision #42. 138 quotes across YBG_QUOTES + YBG_QOTD in softball.html (~line 12549 and ~line 16314). Triggered by spotting 'Your jersey number changes. Your work ethic doesn't.' on Dashboard — logically backwards because work ethic IS the thing a player controls. Read every quote against: (1) athletically true (not just inspirational fluff), (2) age-appropriate for baseball/softball players 11-18, (3) passes no-em-dash + sign-as-Coach-not-Coach-Young rules (Decisions #34 + #35), (4) reinforces something the player actually controls (locus of control test). Pull or rewrite anything that fails. ~60-90 min focused session. The 5-16 cleanup pulled 1 quote (pool 140→138); the remaining 138 still need a pass.\n\n- GameChanger import via backend proxy — deferred to post-launch upgrade tier (UI removed 2026-05-08, dead JS removed 2026-05-09; Option 1 was disabled because Anthropic API is blocked by browser CORS, would need a server-side endpoint)
- MaxPreps integration — deferred to post-launch upgrade tier (UI removed 2026-05-09; revisit once initial app is running and API partnership lands)
- Affirmation push notifications via SMS (Twilio integration)
- Internationalization (Spanish version for the Latino baseball community)

---

## 🐛 Bugs / User Feedback

*When a beta user reports something, jot it here with date and source.*

- (none new yet — start logging when reports come in)

---

## ✅ Recently Shipped (last 7 days)

*Move shipped items here from "This Week" so you have a record of progress.*

### 2026-05-18 (LAUNCH DAY · 32 commits · Cross-Device Sign-In Live)
- ✅ **LANDING FLIPPED TO LIVE** 10:49am PT — `index.html` waitlist form replaced with "Now Live" + primary CTA `/signup.html?promo=FOUNDERMYGRIND` (commit `7b8a1cb`).
- ✅ **4 customer-facing launch bugs found via real testing + fixed within hours**: onboarding→journal canonical key (`e42d220`); trial 7→14 days swept across 13 surfaces (`eed3e36`); defensive trial-grant in journal as safety net (`7dd8e88`); FOUNDERMYGRIND + FOREVERYOUNG2026 grant real access not just copy (`6cb9ae4`).
- ✅ **Cross-Device Sign-In V1 COMPLETE end-to-end** — new `signin.html` passwordless email-link flow (`582c2ac`). Firebase Console configured (Email/Password + Email link passwordless enabled, Authorized domains `mygrindapp.com` + `www.mygrindapp.com` added). Coach verified full round-trip on production. Entry points in signup Screen 0, softball welcome, landing footer, trial-abuse overlay (`64cdb36`), Settings Sign Out card (`a4f7481`).
- ✅ **Cloud backup banner + Settings card** — `bulkBackupToCloud()` uploads existing local journal entries to Firestore on first sign-in (`36a7ae5`, `52ce4a5`).
- ✅ **Trial-abuse Tier 1 LIVE** — atomic email + phone Redis SET NX block on re-signup (`043abec`).
- ✅ **Stripe webhook cancellation email** — closes Settings policy promise (`520668b`).
- ✅ **Option A v1 — Day-11 CC capture** — new `api/create-checkout-session.js` creates Stripe Checkout with `trial_end` set to MyGrind cliff. All behind `OPTION_A_DEFAULT_ENABLED = false` flag. Test plan in commit `8d793aa` message.
- ✅ **Install card permanent in Settings + iOS banner copy fix** — "Scroll down" → "Swipe up in the menu that opens" (`54a1c26`).
- ✅ **Sign Out + Screen 0 cross-device guardrail** (`a4f7481`).
- ✅ **Security audit follow-ups** — feedback endpoints rate-limited, PII hashed in Vercel logs (`36bd2fa`, `dfb2f46`).
- ✅ **Legal entity flip** — "My Grind Sports LLC" language live across privacy/terms/footer/schema, pre-CA-approval with rollback ready (`cd8e17b`).
- ✅ **Football multi-sport architecture** — added then hidden from public funnel for phased Phase 3 rollout (`8f6627b`, `aa20c5f`).
- ✅ **Em-dash sweep (Decision #35) fully closed** — 820 → 663, all remaining instances are internal-only (`48c5d74`, `c3ff11a`, `51f07fe`, `ff8770f`, `12fc62e`).
- 🛑 **Branded magic-link email + one-click Stripe Customer Portal** built then rolled back same night — Vercel Hobby plan caps at 12 serverless functions per deployment, my commits pushed to 14 and build failed. Hotfix `e65ea11` deleted `api/magic-link-request.js`, `api/magic-link-verify.js`, `api/stripe-portal-session.js`, `lib/firebase-admin.js`, and the `firebase-admin` dep. All callers have built-in fallbacks (signin.html falls to Firebase built-in email; Settings portal button falls to hardcoded Stripe login URL; signup dashboard portal link falls to mailto). Recovery: merge magic-link-request + magic-link-verify into single endpoint with `?action=request|verify`, merge stripe-portal-session into get-subscription — both polish features come back AND leaves 1 slot headroom. OR upgrade Vercel to Pro ($20/mo) to remove the cap.

### 2026-05-13 (Founder Offer + Email Stack — 3 commits + heavy ops work)
- Stripe coupon swap: `MYGRIND6` deactivated, new `FOUNDERMYGRIND` promotion code live (0/100, 6 months free). Coupon renamed "MyGrind Founders - 6 Month Free."
- `signup.html` — PROMO_INFO entry swapped, Screen 5 confirmation copy now reads "Founder code applied — your first 6 months are free. Welcome to the first 100." Added `?promo=<code>` URL param handler so launch-day email links land users on Screen 5 with the code already applied and the trial copy already flipped (commits `d229215`, `08eb626`).
- `index.html` — reframed as a founder offer. New `.founder-note` block between hero and form with a personal letter from Coach pitching FOUNDERMYGRIND. CTA copy swaps: "Notify Me" → "Reserve My Spot", "Be First To Know" → "Reserve Your 6 Months Free", trust line + success message rewritten in Coach's voice. Animation extended to fade in the founder note at 500ms (commit `a8fa3bb`).
- Email deliverability — SPF installed via cPanel suggested record, DMARC auto-validated, DKIM + PTR were already valid. All four green.
- Email routing untangled — MX has been pointing to Private Email all along (mx1/mx2.privateemail.com), not jellyfish.systems as old memory claimed. cPanel `coach@`/`support@` mailboxes were dead-letter boxes (could send, couldn't receive); deleted. Private Email confirmed as single source of truth. `coach@` Auto-Forward → Gmail configured.
- Decisions #31, #32, #33 locked (founder-letter voice for pre-launch, Stripe enforces the cap not the page/email, Private Email is sole inbound mailbox system).

### 2026-05-12 (Journal polish + Dashboard reorg + age band audit — 21 commits, SW v145 → v167)
- ✅ **Off Day flow built end-to-end** — 😴 dropdown label "(full rest)", blue info banner, lock system (when an Off Day exists, training types are disabled; only Reflection allowed), hard duplicate guard inside saveEntry (8 duplicate Off Days seen in walkthrough was the trigger), instant unlock on delete wired into renderAll, recursion-safe applyOffDayLock with _applyingOffDayLock flag. Commits `c3775b6`, `a793477`, `723da84`, `a51d7d5`, `8cfe1bb`, `d841895`, `b06d6ec`.
- ✅ **Off Day prompts rewritten — affirmation, recovery, family** — three distinct boxes that don't overlap with Reflection: "Today's affirmation" (seeded examples like "I am the player my team needs"), "How are you taking care of yourself?" (sleep/food/hydration/stretching/ice), "Who or what filled your cup today?" (family/friends/faith/fun). Reflection is addition not replacement — player can save Off Day then add Reflection on same date. Commit `b06d6ec`.
- ✅ **Reflection redesigned (inspired by Day One / Stoic / Reflectly)** — 🪞 intro card with italic invitation copy, mindset emoji picker (🔥 Fired up · 😌 Calm · 🙏 Grateful · 😐 Neutral · 😤 Frustrated · 😞 Down — saved as `entry.mindset`), cream accent (#D4C29B) distinct from action-oriented gold, `body[data-reflection-active]` CSS hook for Lora italic labels + larger 90px textareas, prompts opened to any timeframe ("What's been on your mind?" / "What is this stretch teaching you?" / "What are you carrying forward?"). Title optional. Commits `5fa26b6`, `5e8d799`.
- ✅ **Save Entry button feels like a button** — :active press state on every .btn site-wide (scale 0.97 + darker tint), saveEntry fires haptic + gold `savePulse` animation, universal pointerdown press-flash applied to every button / .btn / a.btn / [role=button] in the app. Commits `80ad87f`, `23a257a`.
- ✅ **Instant entry-list update on save — no refresh needed** — root cause was stale Pick Date / 7d / 30d filter silently excluding the new entry. Right after `state.entries.unshift`, force `_entriesFilter` to 'today' (or 'all' for back-dates), sync filter chip visual state, call renderEntries() directly. Last-resort fallback: if new entry-card not in DOM after render, force 'all' filter and re-render. requestAnimationFrame scrolls #entries-list into view + gold flash on new card. Commits `29b2886`, `187dde9`, `23a257a`, `f005e4d`.
- ✅ **Dashboard reorganized for parent-awareness** — removed Today's Plan card (redundant with Journal as action surface), merged Weekly Recap + Parents Portal into single "👨‍👩‍👦 Share With Family" card (Text + Email + Export PDF buttons + inline weekly body), lifted Coach Comms (New From Coach + Milestone) up under Academics, grouped Hitting Stats + Fielding (previously separated by Photobook), moved Photobook to sit next to Share With Family at the bottom. New top-to-bottom flow: Academics → Coach Comms → Month Focus → Training Volume → Gym → Rest Days → Goals → Hitting → Fielding → Photobook → Share With Family. Commit `6a975e3`.
- ✅ **Rest Days dashboard card** — counts offday-type entries with 7d / month / season buckets, "Last: N days ago" hint, blue accent (#5DADE2) matching Off Day badge palette, hidden until first Off Day exists. Wired into renderAll() so it updates the instant an Off Day is saved or deleted. Commit `0f8e3a1`.
- ✅ **Streak emoji tiers — 🧊 / 🔥 / 💎** — 🧊 frozen at 0.55 opacity (streak=0 AND <3 entries last 7d), 🔥 flame (any active streak OR 3+ entries last 7d), 💎 diamond (streak ≥ 7). Element stays visible at all tiers — cold state actively nudges instead of leaving a blank corner. Commit `f19af11`.
- ✅ **Age band broadened — MS through college** — Profile grade selector added 6th + 7th Grade options (was 8th-grade floor); School label "High School / Team" → "School / Team" with placeholder inclusive of MS/HS/college/travel; goal presets added Make My Middle School Team + Make the JV Team in Roster & Travel, 60+ / 70+ / 75+ MPH pitching tiers below the existing 80/85/90, Sub-8.0 / Sub-7.5 60-yard tiers below the existing Sub-7.0 / Sub-6.7. NCAA recruiting tiles stay as-is. Under-12 deferred to separate product post-launch. Commit `ab4a9b9`.
- ✅ **Modern font stack + touch accessibility audit** — body now ships the native font stack top mobile apps use (-apple-system, BlinkMacSystemFont, SF Pro, Segoe UI, Roboto, Inter, Barlow as legacy fallback) with -webkit-font-smoothing antialiased. Inter + Lora added to Google Fonts request. Three real mobile bugs fixed: input font-size 15px → 16px (kills iOS auto-zoom on focus), touch-action: manipulation on every interactive element (kills 300ms tap delay), -webkit-tap-highlight-color: transparent (removes grey flash on tap). 44px min-height tap floor already in place from 5-11 audit. Commit `c9a65bf`.
- ✅ **Arm Care hidden for Instructor entries + tip note** — instructor explicitly removed from arm-care visibility logic (`updateArmCareVisibility` + `hasThrowingEntryToday`). Tip note added at top of Instructor card: "Show up a little early so you can warm up before your lesson — arm care, light jog, dynamic stretches." Commit `51fc66c`.

### 2026-05-11 (Entry-type expansion + arm care rewrite + pre-launch audit — 14 commits, SW v78 → v92)
- ✅ **Off Day as a 6th entry type (Decision #28)** — 😴 added to the j-type dropdown. Form mirrors Reflection structure (no rep counter, no arm care, no game stats) but with off-day-specific placeholders ("Sunday rest · No throwing today · Family day"). Blue `.type-offday` pill in past entries. Lets a player journal even on no-training days without forcing a fake entry type. Commit `99e9d76`.
- ✅ **Per-entry sport tagging (Decision #29)** — every saved entry stamped with `entry.sport = 'baseball'|'softball'` at save time. `_filteredEntries` filters the entries list by active sport. Past entries without the tag (pre-5/11) are sport-agnostic and show in both views so legacy journals don't suddenly look empty. `editEntry` preserves the original sport. Commit `075308f`.
- ✅ **Sport tabs gated to owned sport** — non-owned sport tab is greyed + `aria-disabled`. `setActiveSport` refuses non-owned switches. Emoji walker no longer rewrites tab icons (softball tab keeps its 🥎 in baseball mode). Switching sport fires `cancelEdit()` to clear the entry form. Commit `ccacd2d`.
- ✅ **Entries-list filter chips** — `[📅 Today] [7 Days] [30 Days] [All] [📅 Pick Date]` above the past-entries list. Today is the default. Pick Date triggers the native phone calendar via `showPicker()` (`date:YYYY-MM-DD` filter format). Past-entries panel no longer pollutes the day-of view. Commits `1c54404`, `0a70a38`.
- ✅ **Streak rule locked (Decision #30)** — every entry type maintains the streak, including Off Day. Documented in a top-of-function comment on `calculateStreak()`. No code change needed since the function already iterates without type-filtering. Commit `6244d70`.
- ✅ **Prior-day delete safety upgraded** — three layers of friction now protect honest history: PIN → confirm modal with "📝 Edit Instead" escape → 10-second undo toast. Strong warning line ("⚠ THIS IS FROM N DAYS AGO") inside the modal for protected entries. Commit `46f85b0`.
- ✅ **Form date picker — back-date banner** — date input stays interactive (back-dating is a real "I forgot to log yesterday" need). Added a loud orange `📅 LOGGING FOR FRI, MAY 8, 2026 · 3 DAYS AGO` banner that activates the moment a non-today date is picked, so back-dating can't happen silently. After save, banner clears + form resets to today + toast confirms (`✅ Saved for May 8, 2026`). Commits `061641b` (lock attempt), `f3cfe81` (restore + banner), `0ef797c` (toast + auto-jump), `efa8be4` (revert auto-jump). Multiple iterations until the right model landed.
- ✅ **Arm Care content rewrite per Coach Young's throwing-day flow** — deleted Wrist Snaps. Added Run Poles, Arm Stretches, Long Toss, Jog After Bullpens, Ice With Pressure. Repurposed "Cool-down toss" → "Walk the ball in." Updated reference modal copy for every changed + new item. Sections: PRE-THROW WARMUP (poles → stretches → mobility → bands) · THROWING (toss → longtoss → walk-in) · RECOVERY (jog → ice → active rest). Removed tier-gating; full list always visible. Commits `cbef7a0`, `9f94c91`, `82262ce`.
- ✅ **Arm Care simplified to a daily reminder** — no mutual exclusion, no auto REST DAY pill. DONE pill at 5/5 + always-available `▲ Hide` button at any state. Coach: "good reminder, not mandatory." Multi-state pill (done / partial / empty) when manually collapsed. Commit `ccacd2d`.
- ✅ **Pre-launch hardening shipped** — photo compression (canvas resize to max 1200px + JPEG 0.82, typical 4MB phone photo → ~150-250KB; prevents silent localStorage cap data loss); 80%-of-cap warning toast; empty-state copy updated to mention Workout / Instructor / Off Day; Phase 7b V1 coach card wiring smoke-tested intact. Commit `6244d70`.
- 📌 **3 Decisions locked** — #28 (Off Day type), #29 (sport-tagged entries), #30 (streak counts every type).

### 2026-05-10 (Daily Journal walkthrough marathon — 28 commits, SW v51 → v78)
- ✅ **Schedule checklists 4/4 complete** — Calendar tab seasonal weekly content now renders as checkable bullets (190 bullets across 48 weeks, parsed via emoji-prefix detection, per-bullet state in `ybg_calendar_checks`). Commit `2150b61`.
- ✅ **Edit-PIN protection (Decision #25)** — entries older than 24 hours require a 4-digit PIN to edit or delete. First-time setup prompts; subsequent attempts verify against hashed `ybg_edit_pin`. 3 wrong attempts → 30-min lockout. Creator (Coach) bypasses. 🔒 icon + "edited Nh ago" badge in entry header. Commit `e85b3f4`.
- ✅ **Delete safety net** — replaced native browser confirm with a custom modal (🗑 Delete / 📝 Edit Instead / Cancel) + 10-second undo toast. Soft-delete holds the entry in memory; UNDO restores it to top of list. PIN-protected entries skip the modal — PIN is the confirmation. Commit `f49ad9a`.
- ✅ **Solo practice as first-class entry sub-type (Decision #26)** — practice context toggle [👥 With Team] [💪 On My Own]. Solo hides Coach Comment + Save Note for Coach (irrelevant), reveals multi-select solo work-type chips (🏌️ Tee / 🏏 Cages / ⚾ Bullpen / 🎯 Long Toss / 🧤 Fielding / 🏃 Running / 🤸 Mobility / 📋 Other). `entry.practiceMode='solo'` + comma-separated `entry.soloWorkType`. Past entries render as `individual · {work types}`. Title fallback: single-chip → label, multi → "Solo Practice." Commits `cca4c1d`, `6e40540`, `5f306f1`, `c2f73d1`, `62f7fb8`.
- ✅ **In-app coach/instructor notes (Decision #27)** — replaced "Text My Coach" SMS button (open-ended, no reply visibility) with always-visible in-app `entry.coachNote` + `entry.instructor.noteForCoach` fields. Caption: "Your coach will see it in the Coach Portal — coming with MyGrind Pro." Notes also surface in the weekly recap to parents. Two-way real messaging deferred to Phase 7b V2 as a paid upgrade. Commits `5d21976`, `ba74f22`, `fb90e7d`.
- ✅ **Future-date block + reps-without-context guard** — entry date picker hard-capped to today (timezone-safe via `todayLocalISO()`). Practice entries blocked from save with reps but no text/grade/photo/comment (prevents streak-padding). Commits `3066082`, `0872e18`.
- ✅ **Grade + Result chips replace dropdowns** — single-letter `<select>` options gave no context when closed. Now: `[A — Excellent] [B — Good] [C — Average] [D — Below Avg] [F — Struggled]` always visible; same chip pattern as the rep counter. Past-entry badges include the descriptor too: `[L · LOSS]` `[F · STRUGGLED]`. Commits `78eaf54`, `d29f46f`.
- ✅ **Typography hierarchy** — `.card-title` promoted from 13px Barlow Condensed eyebrow → 22px Bebas Neue display heading (affects all 40 card titles app-wide). Real H1 (42px) > H2 (22px) > body now. Rep counter chips bumped 12→15 + padding 7/10→11/14 (proper touch target). Past-entry rep tiles: numbers 16-22 → 22-28, labels 9 → 11. Commits `cc5e94e`, `f932753`, `8039bfe`.
- ✅ **Arm care victory pill** — once 5/5 done, the full checklist compresses to a green "Done for Today" pill (frees mobile real estate). Tap to re-expand; "▲ Hide" button to re-collapse. Commits `9172f96`, `2bfecc7`, `0f89180`.
- ✅ **Workout Volume counter on Dashboard** — 7-day / month-to-date / season buckets keyed off the `workout` entry type; "Last: 3 days ago" hint; hidden until first workout. Commit `39d6d9f`.
- ✅ **Past-entries section break** — replaced thin `<hr>` with a brand section header (📓 YOUR ENTRIES + Bebas 30px + gold accent line). Commit `65e7454`.
- ✅ **Rep counter polish** — Exact chip moved to first position (peer first-choice, not fallback); redundant "0" chip dropped (no-tap = 0 anyway). Commits `61f6c91`, `3312812`.
- ✅ **Form polish** — "+ NEW ENTRY" eyebrow stamp removed (redundant after card-title bump); Coach Comment subtitle dropped; date picker timezone bug fixed (was rolling to next day in evening Pacific via UTC `valueAsDate`); date resets to today after save. Commits `fb90e7d`, `f91bc1a`, `62f7fb8`.
- ✅ **Dead UI cleanup** — 5 orphaned functions/blocks removed (`showPaywall`, `saveAllStats`, `pullSeasonStats`, `dash-recent-entries` populator, `season-totals-display` block) + silent regression fix in PDF export (was reading dead `ybg_softball_stats` key; now derives season totals from journal game entries). Net –114 lines. Commit `907153e`.
- ✅ **Verified `setRepFromSavedValue()`** — Exact-mode fallback handles pre-2026-05-09 broader rep-chip buckets correctly. Comment added documenting the contract. Commit `2f24ee6`.
- 📌 **3 Decisions locked** — #25 (edit-PIN), #26 (solo as first-class), #27 (two-way coach deferred to MyGrind Pro).
- 🐛 **2 latent bugs caught + fixed** — date-picker timezone rollover, inline-style CSS specificity overriding `.selected` chip background.

### 2026-05-07
- ✅ **SEO foundation complete (NEW phase)** — title/description/OG/Twitter Card rewritten on `index.html` for "MyGrind app" + sport keywords. JSON-LD `@graph` with 3 nodes (Organization + WebSite + SoftwareApplication with 3 Offer prices). 1200×630 OG card at `/assets/og-card.png` generated via Canva using saved brand kit + web logo asset (LinkedIn preview verified). Vercel Web Analytics deployed across all 6 public pages (cookieless, privacy.html §11 compliant). Google Search Console URL-prefix verified via `/google826fc67b2dde2efa.html`; `sitemap.xml` updated with `lastmod` and submitted to Search Console with **Success** status. Commits `1513011`, `d49a88a`, `97b71b3`.
- ✅ **Signup flow polish** — removed redundant `<div class="badge">` from S1–S7 + redundant right-side prog-label spans across all signup screens (consistency: H1 already conveys section). Brightened `.field-label` + `.field-helper` colors; bumped global H1 (42→46, mobile 38→44) and `.sub` (15→16, mobile 17→18). Brand wordmark on Screen 0 promoted from small badge to a proper 26px gold wordmark. ⚡ icon added to Get Started CTA. Phone hardware back-button now navigates between screens via `history.pushState` + `popstate` listener. Promo-aware trial copy: when `MYGRIND6` or `FOREVERYOUNG2026` is entered on Screen 5, the sub line, "What happens at day 14?" promise block, and Screen 7 reminder line all rewrite to match the actual free window (6 months / lifetime); lifetime promos hide the "skip the trial" link entirely. Screen 7 "What's next" block resized to actually be seen. Screen 8 dashboard footer links bigger + brighter; modal typography enlarged; QR code option added to Share modal. Player-card preview brightened with gold-lock signal + 3 new engagement rows (coach feedback, season stats, split games/practices). Journal preview row reframed: visible count + "text stays private". Commits `673c355`, `2848144`, `75ba451`, `4bc9353`, `04f96e5`, `4c426e6`, `fd3df6e`, `8b10d67`, `996a894`.
- ✅ **Onboarding rebuilt with direct softball.html handoff** — `onFinalExit()` now navigates straight to softball.html (was: showed a stale "your dashboard is shipping soon" holding overlay; the entire `showFinalHoldingScreen` function + ~50 lines of dead code deleted). New "Open My Journal →" CTA on S14 replaces the old "Got It — I'll Be Back". Explicit journal-privacy promise added to S14 ("what you write in your journal stays private to you" — mirrors parent-side dashboard preview). Onboarding completion now writes `ybg_softball_access.trialStart = Date.now()` to localStorage so softball.html's `validateSession()` returns `'trial'` instead of bouncing to signup (Locked as **Decision #23**). Killed "You're In ⚾ Baseball" progress-bar text label across all 12 onboarding screens. Bumped "Now it's on you" headline on S13 (34→48px). Commits `fdc1017`, `fdbb8d1`, `d709d66`, `82a1293`, `15a9fab`, `6fed9dc`.
- ✅ **softball.html walkthrough started** — added `youngsbaseball@gmail.com` to `CREATOR_EMAILS` and a permanent `?unlock=1` URL-param creator bypass (handler runs at top of `init()` before the existing creator-flag cleanup, sets both `ybg_creator` AND `ybg_pin_unlocked`). Header logo enlarged + filtered to white (mark 44→60px, wordmark 32→42px). `.section-sub` bumped 13→16px and brightened. Daily Journal panel header cleaned (dropped redundant "Post-Game · Practice · Reflection" subhead + orphan gold-line). Commits `3330538`, `02b638b`, `473c2f3`, `34af5e1`.
- ✅ **`docs/ANALYTICS.md` shipped** — single reference doc covering Vercel Analytics dashboard, Stripe trial/paid views, Google Search Console performance, OpenGraph validator, weekly + monthly review checklists, and automation upgrade paths (weekly Notion pulse, Slack/email digest — both queued, neither built yet). Commit `df4cce3`.
- 📌 **Locked Decision #23** — Player onboarding completion auto-grants softball.html trial access. Previously the trial gate was separate from onboarding (double-gate caused tonight's redirect loop). Now: `onFinalExit()` writes `ybg_softball_access.trialStart` if not already paid/active. Trial-overlay welcome screen still exists for direct-link landings.

### 2026-05-06
- ✅ **Stripe webhook repaired + reconciled** — Vercel "Sensitive" env vars (STRIPE_SECRET_KEY, STRIPE_WEBHOOK_SECRET, STRIPE_PUBLISHABLE_KEY) had been saved with empty values during yesterday's signing-secret rotation, causing all webhook deliveries to return HTTP 500 `server_misconfigured`. Re-pasted via dashboard, redeployed, verified by Aaron Pulley's `customer.subscription.deleted` event auto-recovering to 200 OK at 7:34 AM. Patched all 4 silent-no-op `if (!email) break;` paths in `api/stripe-webhook.js` to `console.warn` with relevant Stripe IDs.
- ✅ **Path A reconciliation (Family pricing)** — discovered live Family Annual payment link charges $149.99/yr (not $149). Updated webhook PRICE_TO_PLAN map to live IDs (`price_1TT69tPm4...` family_monthly, `price_1TT68UPm4...` family_annual). Archived unused canonical `prod_US01TjY0XREZtc` "MyGrind Family" + 2 prices in Stripe. Family copy bumped $149 → $149.99 in 5 places in `signup.html` + 3 doc files (`CLAUDE.md`, `docs/ROADMAP.md`, `docs/STATUS.md`). Derived math fixed: `~$12.42/mo` → `~$12.50/mo`, `save $148/yr` → `save $147/yr`. Locked as **Decision #21**.
- ✅ **softball.html broken paywall fixed** — all 3 `STRIPE_*_URL` constants pointed at INACTIVE Stripe Payment Links (anyone tapping Subscribe got a "this link is no longer active" Stripe error). Updated to use signup.html's working URLs for Single Monthly + Single Annual; created new `plink_1TU78PPm4ermqky4nUSQdVmb` for $300 Team Sponsor.
- ✅ **Adult self-signup funnel (Scope X)** — `signup.html` now opens with a Screen 0 audience picker ("My Athlete" / "Myself"). Self path: `state.signupFor='self'` + `applySelfLabels(n)` swaps copy on Screens 1-4 + playerCount question hidden on Screen 3 + Screen 6 auto-fills player record from parent state and skips to Screen 7. URL handoff `?self=1` to `onboarding.html` triggers self-aware PIN screen, welcome subhead, end-of-flow copy. End-of-onboarding heading swapped from "See you tomorrow" → "Let's grind". Locked as **Decision #22**.
- ✅ **Multi-sport platform Phase 1** — `softball.html` refactored into a sport-aware shell themed per `data-sport`. Central `SPORTS` config (baseball/softball/both with id/label/slug/emoji/accent/accentRgb), `getSportConfig()` helper, exposed via `window.SPORTS` / `window.getSportConfig` / `window.mgSportConfig`. 127 `#D4547A` + 8 `#E87FA0` + 4 `#A03060` + 38 `rgba(212,84,122,X)` → `var(--gold)` family (165 sport-aware refs total). 16 hardcoded 🥎 → ⚾ as HTML defaults; 3 JS-context emoji uses (Notification × 2, drill icon) refactored to runtime `mgSportConfig.emoji`. Bidirectional `applyBallEmoji()`. New `window.setActiveSport(s)` global re-themes live (data-sport attr + theme-color meta + emoji walk + {Sport} template re-render). Multi-sport toggle UI in dashboard header (gold-pilled `⚾ Baseball | 🥎 Softball`, hidden by default, auto-revealed via CSS when `<html data-family-sport="both">`). New `mg_family_sport` localStorage key tracks original signup choice. signup.html `selectFamilySport()` writes the same key so dashboard knows when to surface the toggle. Recipe doc `docs/ADD_A_SPORT.md` makes new sports a ~15-minute job. "Coming soon for: 🏈 🏐 🏀 ⚽" strip on Screen 0 signals expansion. Locked as **Decision #20**.
- ✅ **Promo code system** — collapsible `+ HAVE A PROMO CODE?` gold-pilled button on Screen 5 of `signup.html`. Generic "ENTER CODE" placeholder. Auto-uppercases. Persists to `state.promoCode`. New shared `buildCheckoutUrl()` helper appends `prefilled_promo_code=` + `prefilled_email=` to Stripe Payment Link URL. Both checkout entry points (`onSkipTrialClicked` + `onDashboardPayClicked`) use it. Restore logic on Screen 5 entry pre-fills + auto-expands the dropdown if a code is saved.
- ✅ **`FOREVERYOUNG2026` lifetime free code created** — Stripe coupon `CQoNpVpK` (100% off, duration: forever), promotion code `promo_1TUIriPm4ermqky4RpZVI42Y`. Capped at **10 redemptions**, expires 2030-12-31. For hand-picked recipients. Card still collected at Stripe checkout but never charged.
- ✅ **Trial banner removed from Screen 8 dashboard** in `signup.html`. "14 Days Free" was redundant (parent saw it twice already on Screens 1+5) + risk of any player picking up the parent's phone seeing billing copy.
- ✅ **Defensive horizontal-scroll CSS** in `signup.html` — `min-width: 0` on `.app`/`.screen`/`.screen-body` (fixes flex-child auto-min-width gotcha). `body { max-width: 100vw }`. Plus `img/video/iframe { max-width: 100% }`.
- ✅ **Logo + font bumps** for adult readability on phone — logo 96px → 140px height + 80vw → 90vw on `signup.html` + `onboarding.html`. Phone-only @media block bumps `.sub` 16→17, `.tap-card-desc` 13→15, `.goal-toggle-text` 14→16, `.field-input` 16 (kills iOS zoom-on-focus). Same media block added to `onboarding.html`.
- ✅ **`docs/ADD_A_SPORT.md` recipe doc** — 5-step guide (SPORTS entry + CSS block + signup tile + optional content bundle + optional landing page) with checklist, existing-sports table, and "what won't break per-sport" notes. Targets ~15-minute time-to-add for football, volleyball, basketball, soccer, etc.

### 2026-05-05
- ✅ **Phase 5 Steps 2-4 — Stripe webhook + dashboard pay + subscription sync.** New `api/stripe-webhook.js` (signature-verified, handles 5 event types), new `api/get-subscription.js`, new `lib/subscription-store.js` (Redis CRUD by email, idempotent on `lastEventId`). Dashboard "Pay early" link added to signup.html Screen 8. softball.html now syncs paid status from server on every app load. Webhook destination "fascinating-oasis" live in Stripe Workbench. Commit `bbe6cbf`.
- ✅ **Phase 7b V1.5 — parent weekly email digest** shipped end-to-end and verified by real Gmail delivery. New `lib/email-digest.js` (warm-dark branded HTML, groups by player), new `api/cron/weekly-digest.js` (Vercel cron, SCANs Redis for active parents, sends via Resend, auth via `Bearer ${CRON_SECRET}`), new `vercel.json` (Monday 14:00 UTC schedule). Test mode: all emails redirected to youngsbaseball@gmail.com via `WEEKLY_DIGEST_TEST_EMAIL` env until `mygrindapp.com` is verified in Resend. Sender: `onboarding@resend.dev`. Commits `8881d38`, `bd03455` (chip rendering fix for `{key,label,detail}` objects).
- ✅ **mygrind-visuals skill** added at `~/.claude/skills/mygrind-visuals/SKILL.md`. Orchestrates Higgsfield image + video generation with MyGrind brand guardrails (Warm Dark palette, parent-first voice, banned-words list, model decision tree, prompt template). Auto-fires on any MyGrind visual request.
- ✅ **MyGrind logos uploaded to Higgsfield** — 4 variants (master 5692×3200, web 920×517, mark 902×907, app icon 179×180). Media IDs saved to memory `higgsfield_mygrind_media.md` and wired into the skill so generations default to the right logo for context.
- ✅ **Launch creative generated** — 2× kling3_0 Reels (player finishing BP → opens MyGrind on phone, with voiceover and ambient sound), 2× nano_banana_2 photorealistic IG posts (14-yo boy journaling in dugout at golden hour). Job IDs visible in Higgsfield generations panel.

### 2026-05-04
- ✅ **Phase 7b V1 — end-to-end coach feedback loop** shipped + verified against production. Player → SMS magic link → coach taps → coach-reply.html → coach picks chips + comment → SMS player + parent weekly review card on dashboard. NEW: `lib/feedback-store.js` (Redis-backed CRUD), 4 API endpoints (`feedback-request`, `feedback-get`, `feedback-respond`, `feedback-list`), `coach-reply.html`. Updates: softball.html "💬 New from Coach" dashboard card, signup.html Screen 8 "📋 This Week's Coaching" parent card. Token-gated writes (32-hex secret), 90-day TTL. SMS via Twilio (DRY_RUN until TFV approves). Commit `c00d3a9`.
- ✅ **Phase 7a — player Coach Feedback tab** shipped (mailto V1) earlier in the day; superseded by 7b V1. Commit `bb74304`.
- ✅ **Twilio TFV submitted** 2026-05-04 via console. Privacy §10 + Terms §9 added with full SMS compliance language (consent, frequency, STOP/HELP, retention, carrier disclosure, parent authority for player phone). Calendar check-in May 11 9am PT. `SMS_DRY_RUN=true` stays until Twilio shows APPROVED. Commit `b277a45`.
- ✅ **Logo brand cleanup** — replaced text "MY GRIND" / "MG" with real logo assets across the entire app. `assets/apple-touch-icon.png` + `assets/favicon.png` regenerated from current `mark.png` (was old red/black design); inline base64 for splash + header re-encoded too. Commits `3b8f48c`, `a9978b9`, `6a5c5db`.
- ✅ **All customer-facing email → support@mygrindapp.com** — 59 replacements across signup/softball/terms/privacy + CREATOR_EMAILS access list (admin login flips to support@). Backup admins (papamike@, michael@youngsbaseball.com) preserved. Commit `b92d7dc`.
- ✅ **P0 security audit complete** — CREATOR_PIN backdoor (was hardcoded "YBG2026") removed from softball.html; CORS allowlist locks `/api/send-invite` to mygrindapp.com origins only; Firestore rules verified auth-required + uid-scoped; `SMS_DRY_RUN=false` confirmed missing in Vercel Production (intentional for pre-TFV). Commit `e74e05d`.
- ✅ **Phase 2 — Sport-aware copy** in softball.html. Title/meta/OG/body all swap on `?sport=` via head-script template system (`data-mg-text` attribute + `window.mgSportName`). Sport-aware ⚾↔🥎 emoji swap on softball.html + onboarding.html. Commit `d8cf6c5`, `c7f117c`, `7f98d7a`.
- ✅ **Screen 8 stale-player-cards bug fixed** — 5th render path with the same trim bug as Screens 6/7 (selectPlayerCount, buildScreen7, buildPhoneConfirmList, getFallbackPlayerNames). Now slices `state.players` to `playerCount` at render time. Commit `4f6aa32`.
- ✅ **YBG → MyGrind newsletter merge** decision locked. Issue #1 of "The Grind Weekly" drafted in Notion (`345819955e6f817181bdd396d9fb1c41`) ready to send 2026-05-05 7am PT — announces the brand merge at top, includes full MyGrind app intro + pricing + MYGRIND6 promo, scout/recruiting training topic, 60-yard dash drill, new permanent "For the Parents" section, YBG travel team CTA, Pocket Radar gear pick. After send: YBG Mailchimp audience absorbed into MyGrind audience tagged `ybg-legacy`, YBG audience archived.

### 2026-05-03
- ✅ **Phase 5 Step 1 — Skip-trial Stripe redirect wired** in `signup.html`. `onSkipTrialClicked()` redirects to one of 4 live Payment Links (Single/Family × Annual/Monthly) based on `state.preferredPlan` + `state.playerCount`, with `?prefilled_email=` from the parent's typed email. Steps 2-4 (webhook + dashboard pay flow) deferred pending baseball-side fix. (uncommitted in working tree)
- ✅ **Baseball Side Phase 1 — palette swap** in `softball.html`. Sport-detection script in `<head>` reads `?sport=` URL param → `mg_sport` localStorage → default baseball, sets `data-sport` on `<html>` synchronously before paint, updates theme-color meta. `:root` CSS variables (`--gold`, `--accent-rgb`) with `[data-sport="softball"]` override. Splash circle + diamond pattern bg now swap with sport. (uncommitted)
- ✅ **Baseball Side Phase 3 — per-player sport handoff** across `signup.html`, `api/send-invite.js`, and `onboarding.html`. Screen 6 in signup gets a per-player sport tile picker (Baseball / Softball / Both) shown only when `familySport === 'both'`; otherwise auto-derives from family sport. `buildSmsForPlayer(player)` builds invite URL with `&sport=`, matching `api/send-invite.js`. `onboarding.html` mirrors the Phase 1 detection script + `:root` palette tokens; 33 `#C9A84C` → `var(--gold)`, 11 `rgba(201,168,76,…)` → `rgba(var(--accent-rgb), …)`. `parseUrlParams()` accepts `?sport=baseball|softball|both`; `firstFlowScreen()` skips S1 sport-picker when sport is pre-filled. `setSport()` (when kid does pick) propagates to `data-sport` + localStorage + theme-color. (uncommitted)
- ✅ **YBG branding removed** from visible UI in `softball.html`. 8 `>YBG</div>` instances → `>MG</div>` (splash, header, team logos). Team-create placeholders updated to generic examples (`Lions Travel 14U`, `ABC`, etc.). Default team name `YBG TRAVEL` → `YOUR TEAM`. Fallback abbreviation `'YBG'` → `'MG'`. Internal references intentionally kept: `ybg_*` localStorage keys (would break testers), `CREATOR_PIN = 'YBG2026'` (admin access), `YBG_QOTD` variable, console prefixes, cache name. (uncommitted)
- ✅ **State trim bug fixes** in `signup.html`. `selectPlayerCount()` now trims `state.players` when count drops. Render-time slice in `buildScreen7()`, `buildPhoneConfirmList()`, `getFallbackPlayerNames()` so stale slots from prior sessions don't leak into invite previews (visible bug: confirm modal showed Lucas + Sofia when only 1 player picked). (uncommitted)
- ✅ **`?reset=1` URL handler** in `signup.html` for clean test runs — wipes `mg_signup_state`, `mg_sport`, `ybg_creator` and lands at Screen 1. Strips the param from URL bar after wipe. (uncommitted)
- ✅ **SEO baseline** — H1 hardcoded, canonicals (www), noindex on funnel pages, robots.txt, sitemap.xml. Commits `c864e00` + `e238322`.
- ✅ **Phase 5 preflight 🟢 GO** — verified Stripe account, all 6 prices live, webhook endpoint configured, all 3 Stripe env vars set in Vercel Production. Two non-blocking flags surfaced: 2 duplicate Family products to archive in Stripe dashboard, 3 legacy Stripe URLs in `softball.html` (lines 3662-3664) need separate audit.
- ✅ **Locked Decision #16** — "Both" sport means a single multi-sport athlete (one kid who plays baseball AND softball), NOT a family with mixed-sport kids. Refines Decision #7.
- ✅ **Pre-launch infrastructure** (morning marathon): `/morning-sync`, `/eod-sync`, `/save-prompt`, `/publish-prompts`, `/phase5-preflight`, `/twilio-tfv-prep` skills built. 73 read-only MCP tools pre-approved. Scheduled tasks armed (5:10am + 9:08pm PT). Drive prompt library shipped. Canva launch carousel V2 (10 slides) on Coach's Canva account. MyGrind Family product + 4 Payment Links + `MYGRIND6` promo (250 cap, expires 2026-09-01) wired in Stripe.

### 2026-05-02
- ✅ **Family pricing tier shipped** — added a $149.99/yr · $14.99/mo flat household plan covering 2-3 players, alongside the existing Single tier ($99/yr · $9.99/mo per player). Screen 5 pricing cards render dynamically via `renderPricingCards()` based on `state.playerCount` (1 → Single, 2-3 → Family) with a "You Save $X" line below each Family card. Anti-abuse defenses: per-plan phone uniqueness check (no two players share the same real number; fallback exempted), slot-lock fires on first SMS-send (Screen 7 entry → `state.playersLockedAt` ISO timestamp), navigating back to Screen 6 after lock shows a read-only view with a gold banner pointing users to `coach@mygrindapp.com` for swaps. Settings modal updated to explain the swap-via-email policy. Stripe URLs deferred to Phase 5.
- ✅ **Phase 6 — Real Share + Settings modals on signup dashboard** — replaced the two `alert('coming in Phase 6')` placeholders on Screen 8 with proper modals reusing the existing `.phone-confirm-overlay` shell. Share modal has a copy-link button (Clipboard API + execCommand fallback), a Web Share API "Share…" button shown only on supporting browsers, and a pre-written share caption. Settings modal shows read-only account summary (name, email, phone, plan, players) populated from `state.parent` + `state.preferredPlan` + `state.playerCount`, plus a `mailto:coach@mygrindapp.com` link for change requests until Phase 5 wires backend editing.
- ✅ **Phase 3d — Twilio Lookup pre-check** — new `lib/lookup.js` module + integration in `api/send-invite.js`. Calls Twilio Lookup v2 with `line_type_intelligence` between E.164 normalization and rate-limit checks. Rejects invalid numbers, landlines, and VoIP with friendly user-facing messages before burning rate-limit budget or paid SMS sends. Fail-open on Lookup outage. `SKIP_LOOKUP=true` env override for dev. Cost ~$0.005/lookup, bounded by upstream rate limits.
- ✅ **Pre-launch waitlist landing page** at `mygrindapp.com` — brand-styled `index.html` with Mailchimp form (audience `73280b4c02e9bc56c7e633892`, tag `2216797`). Replaced the old redirect shell that bounced visitors to signup.html.
- ✅ **Mailchimp JSONP integration** — replaced optimistic iframe submit with real `subscribe/post-json` call so users see actual success/error messages instead of silent failures.
- ✅ **Mobile sizing fixes for landing** — compact email input (34px tall), narrower form card on portrait, landscape media query, vertical centering with `safe center`, iOS Safari rotation-zoom fix via `maximum-scale=1`.
- ✅ **Disabled Mailchimp reCAPTCHA on the form** so JSONP path doesn't bounce real users into a challenge that can't render.
- ✅ **Critical CSS corruption fix in `onboarding.html`** — `.screen`, `.screen.on`, `.screen-body`, `.prog-wrap` rules had nested braces and orphan property remnants from a prior find/replace incident. Restored intended layout.
- ✅ **`saveState()` data-wipe killed** in `softball.html` — the catch block was silently writing `entries: []` on quota errors. Now leaves previous save intact and surfaces a one-time alert pointing to backup.
- ✅ **GameChanger Option 1 disabled** — the screenshot reader called Anthropic API directly from the browser (CORS-blocked, no key). Replaced with "Coming Soon" notice pointing to Option 2 (manual entry).
- ✅ **Backup & Restore feature re-shipped** in `softball.html` Settings tab — JSON download/upload of all `ybg_*` keys (auth/session keys denylisted). Includes "last backup: X days ago" display.
- ✅ **Backup nudge re-shipped** — non-blocking gold banner appears on app load when user has 3+ entries AND no backup in 7+ days. Dismissable for the session.
- ✅ **Brand system doc** at `docs/BRAND_SYSTEM.md` documenting design tokens for future MyGrind web surfaces.
- ✅ **`media/` folder** added for launch graphics + social assets.
- ✅ **STATUS.md + ROADMAP.md rewritten** to reflect current architecture (signup/onboarding/softball/landing) instead of the old single-file index.html state.

### Earlier (rolled into this list when I clean up)
- ✅ Tab reorder (daily-use features up front)
- ✅ Instructor Session log in Daily Journal
- ✅ Daily affirmation banner (60 affirmations rotating)
- ✅ Instructor as entry type (focused form when selected)
- ✅ Universal "NO GRADES · NO GAME" banner (sport-neutral)
- ✅ Pink palette for softball mode
- ✅ 20 softball-specific quotes
- ✅ Custom academic classes
- ✅ Cloud backup (manual Save to Cloud)
- ✅ Sport selector on trial signup
